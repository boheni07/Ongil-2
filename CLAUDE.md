# 온길 플랫폼 — CLAUDE.md

## 하네스: 온길 플랫폼 개발

**목표:** 장애인 생애주기 기록 플랫폼 온길을 Next.js + React Native + Supabase로 구축

**트리거:** 온길 기능 구현, 화면 추가, API 작성, DB 수정, RLS 정책 등 개발 요청 시 `ongil-dev` 스킬을 사용하라. 단순 질문(문서 읽기, 설명)은 직접 응답 가능.

## 핵심 문서 (docs/)

| 문서 | 내용 |
|------|------|
| `docs/01-prd.md` | PRD — 기능 요구사항, 우선순위 |
| `docs/02-ia.md` | IA — 전체 화면 구조, 88+81 화면 인벤토리 |
| `docs/03-uiux.md` | UIUX — 디자인 시스템, 컴포넌트 사양 |
| `docs/04-workflow.md` | Workflow — 사용자·기능별 플로우 |
| `docs/05-erd.md` | ERD — 13개 테이블 스키마, RLS 정책 |
| `docs/06-wbs.md` | WBS — Phase 0~3 작업분해, 의존성, 에이전트별 AI 실행 프롬프트 |
| `docs/07-lifecycle-record-permission-proposal.md` | 생애주기 5단계 개정 제안(채택 완료) — 채택 안 된 잔여 갭은 §5 참조 |
| `docs/08-record-taxonomy-workshop.md` | 기록 체계 재설계 워크숍 협의록 — PM팀·이해관계자 합동 논의로 확정한 16종 record_type 최종 목록(§5) |
| `docs/09-cto-team-gap-analysis.md` | CTO팀(10인) 전 항목·분야별 갭분석 결과보고서·작업계획서 — 모바일 파리티·pgTAP 하네스·RLS 교차검증 등 8건(§4/§5) |
| `docs/10-livinglab-uiux-workshop.md` | 리빙랩 UIUX 워크숍 — 로그인 후 화면 디자인 격차 진단(UIUX팀+6개 사용자 그룹) + elevation 토큰 신설 등 Wave UI-1~4 개선안 |
| `docs/11-livinglab-mega-workshop.md` | 리빙랩 확대 워크숍 — PM·PL·CTO팀 10인+사용자 12명(역할별 2명) 합동, 모바일 UIUX/기능 파리티 격차·OAuth 우선순위 등 Wave M-1~5 대폭 개선계획 |
| `docs/12-content-fidelity-workshop.md` | 콘텐츠 정합성 워크숍 — 화면 존재 여부를 넘어 카드 내부 콘텐츠 구조·타이포그래피 레벨로 프로토타입 대조(G-01 보호자 대시보드 정밀 해부 Exhibit A), Wave A(백엔드 목록화)~C(확장 적용) 개선계획 |
| `docs/13-cross-role-person-selection-workshop.md` | 역할별 "당사자 선택" 방식 교차 검토 — 보호자(당사자 중심)vs전문직군(업무 중심) 가설 검증, "매번 재선택"은 안전장치로 유지하되 최근 선택 힌트로 보완하는 하이브리드 결론, Wave Q-1·Q-2·Q-3·Q-4·Q-6 완료 — Q-5(당사자 알림 경로)만 미착수 |
| `docs/14-weekly-tasks-workshop.md` | 전문직군 홈 "이번 주 처리할 일" 설계 워크숍 — 역할별 실제 마감일 데이터 전수조사(특수교사·사회복지사는 마감일 기반, 치료사·활동지원사는 백로그 기반으로 이원화), Wave W-1~W-4 전체 완료(웹+모바일) |

## 에이전트 팀 (.claude/agents/)

| 에이전트 | 역할 |
|---------|------|
| `orchestrator` | 전체 개발 조율 |
| `frontend-dev` | Next.js 웹 화면 |
| `mobile-dev` | React Native 모바일 |
| `backend-db` | Supabase DB + API |
| `security-rls` | RLS 정책 + PIPA |
| `qa-verifier` | 갭 분석 + 검증 |

## 기술 스택 요약

- **웹:** Next.js App Router · TypeScript strict · Tailwind CSS · shadcn/ui
- **모바일:** React Native (Expo)
- **DB:** Supabase (PostgreSQL + RLS) · Prisma ORM
- **공통:** TanStack Query · React Hook Form + Zod · pnpm workspaces

## 구현 현황

**Phase 0~3 대부분 진행 중** (2026-07-17 갭 분석 기준) — `apps/web`, `apps/mobile`, `packages/shared`, `packages/validation`, `supabase/prisma`, `supabase/tests`, `supabase/functions` 모두 실체가 있다. Prisma 모델 14개(+invitations), 마이그레이션 19개, pgTAP 테스트 18개(`00~17`), web 라우트 50여 개, mobile 화면 45개가 구현되어 있다. `docs/06-wbs.md` 기준 P0-1~P0-7·P1-1~P1-9·P2-1~P2-8·P3-3/3-4/3-5/3-6/3-8은 코드 근거 확인됨. **P3-7(카카오/네이버 OAuth)은 2026-07-18 코드 구현 완료**(F-AUTH-02) — 다만 카카오/네이버 개발자센터 앱 등록(외부·플랫폼 소유자 전용 작업)이 없어 실제 로그인 end-to-end는 라이브 미검증 상태다. docs/02-ia.md 화면 인벤토리(88 web + 81 mobile) 대비 정확한 화면 단위 매핑 검증은 미완료 — 필요 시 `qa-verifier`로 재검증한다.

## 변경 이력

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-07-07 | 초기 하네스 구성 | 전체 | 온길 플랫폼 개발 시작 |
| 2026-07-09 | 생애주기 3단계(life_stage) 계산 로직·화면 배지 설계 | `01-prd.md` §3-1, `05-erd.md` §2-2-1, `02-ia.md` §3-9, `03-uiux.md` §6-6, `04-workflow.md` Flow-SYS-06 | 아동기/청소년 전환기/성년기별 기록·동의 주체 분기 필요 |
| 2026-07-09 | 권한 매트릭스 권장안 및 관리 라이프사이클(부여·수정·회수·감사) 정식화 | `01-prd.md` §3-2~3-3, `05-erd.md` §4-5 | 역할별 기본 프리셋·감사 절차 부재 |
| 2026-07-09 | 보호자 기록 직접 작성·수정 화면(G-21) 추가, `records_update` RLS 정책 신설 | `02-ia.md`, `01-prd.md` F-G-04, `05-erd.md` §4-2, 프로토타입 `web/app-guardian.html` | 프로토타입 점검 중 보호자가 기록을 작성할 수 없는 설계 누락 발견 |
| 2026-07-09 | 기록 확인(Confirmation) 절차 신설 — "승인"이 아닌 "확인" | `01-prd.md` §3-4, `05-erd.md` §4-6, `02-ia.md` §3-10, `03-uiux.md` §6-7, `04-workflow.md` Flow-SYS-07, 프로토타입 guardian/person | 공식 문서(IEP·ISP·치료계획서) 제출 시 보호자·당사자 인지 절차 필요 |
| 2026-07-09 | 설계문서 정합성 점검·보완 (FR 번호 재정렬, `notification_preferences`·재확인 트리거 정의, Prisma 관계 보완, `record_type` JSONB 스키마 5종 추가) | 전체 `docs/` | 반복 수정으로 누적된 문서 간 불일치(§ 번호 오류, dangling reference) 해소 |
| 2026-07-09 | WBS 신설(`06-wbs.md`) — Phase 0~3 32개 작업, 의존성, 에이전트별 AI 프롬프트 | `docs/06-wbs.md` 신규, `01-prd.md` §7 Phase 0 테이블 수 정정(15→13) | 실제 코드베이스가 전무한 상태에서 실행 계획·일정이 부재 |
| 2026-07-09 | "구현 현황" 섹션을 "Phase 0 완료" → "미착수"로 정정 | `CLAUDE.md`, `01-prd.md` §7 Phase 0 제목 | 하네스 재감사 중 WBS의 "코드베이스 전무" 전제와 CLAUDE.md의 "완료" 표기가 상충하는 것을 발견 |
| 2026-07-10 | P1-3/4/5 백엔드 구현 — 검증 스키마(`records.ts`·`persons.ts`) + Server Action(`home`/`journal`/`dashboard`). 셀프 가입 당사자 모델(`persons.id=primary_guardian_id=auth.uid()`) 채택 | `packages/validation`, `apps/web/src/app/(app)/{home,journal,dashboard}/actions.ts` (신규), `05-erd.md` §4-1/§4-2(설계 기확정) | 당사자 자기표현·활동지원 일지·보호자 대시보드/당사자 등록 구현. person 역할 자가등록 RLS 갭 해소 전제 |
| 2026-07-10 | `invitations` 테이블 신설(Flow-1 이해관계자 초대) + `accept_invitation()` SECURITY DEFINER 전개 함수 | `05-erd.md` §2-12~2-13, `supabase/prisma/schema.prisma`, 마이그레이션 `p0_7_invitations`(테이블) | Flow-1이 요구하나 스키마 누락. invitations/consents RLS·GRANT는 security-rls 마이그레이션(`p0_7_consents_invitations_rls`)이 신뢰 소스 — invitations는 authenticated 한정, anon 미리보기는 service-role 경로로 처리(anon SELECT 시 테이블 덤프 위험) |
| 2026-07-11 | P1-8 RLS pgTAP 스위트 신설(`supabase/tests/00~08`, §4 전체 정책 커버) 중 `permission_logs` RLS 완전 비활성 + authenticated 전체 GRANT 결함 발견·핫픽스 | `05-erd.md` §4-4, 마이그레이션 `p1_permission_logs_rls_hotfix` | consents·guardians와 동일 계열의 실사용 보안 결함 — 임의 로그인 사용자가 전 당사자의 권한 변경 감사 로그를 열람·수정·삭제 가능했음. RLS 활성화 + 주보호자 한정 SELECT + UPDATE/DELETE 정책·GRANT 회수로 봉쇄 |
| 2026-07-13 | S-20/S-21(인수인계) 구현 라운드 중 `handover_notes`·`notifications` RLS 완전 비활성 + authenticated 전체 GRANT 결함 발견·핫픽스 + pgTAP 09 신설(20 asserts) | `05-erd.md` §4-10/§4-11, 마이그레이션 `p2_handover_notifications_rls`, `supabase/tests/09_handover_notifications.sql` | consents·guardians·permission_logs와 동일 계열 결함 — 임의 로그인 사용자가 전 당사자의 인수인계 노트·알림을 열람·위조·삭제 가능했음. RLS 활성화 + 본인 한정 SELECT + INSERT 권한검증(handover: DAI write/보호자, from 위조 차단)·컬럼 단위 GRANT(handover.acknowledged_at, notif.is_read/read_at + insert 안전컬럼)·DELETE 전면 차단으로 봉쇄 |
| 2026-07-15 | qa-verifier 갭분석이 `notification_preferences`·`permission_presets` RLS 완전 비활성 + authenticated 전체 GRANT 결함 발견(**이 프로젝트 6번째 반복 동일 계열 결함**)·핫픽스 + pgTAP 16 신설(12 asserts) + **메타 회귀 테스트 pgTAP 17 신설**(전 테이블 RLS 강제, 1 assert)로 근본 원인 차단 | `05-erd.md` §4-13/§4-14/§4-메타, 마이그레이션 `p3_notif_prefs_presets_rls`, `supabase/tests/16_notification_preferences_permission_presets_rls.sql`·`17_meta_all_tables_rls_enabled.sql` | guardians·consents·permission_logs·handover_notes/notifications에 이은 6번째 동일 결함 — 임의 로그인 사용자가 타인 알림 채널 설정을 변조(보안 알림 무력화)하거나 위자드 참조 프리셋을 UPDATE(과다권한 주입)/DELETE(위자드 파손) 가능했음. notif_prefs는 RLS+본인 한정(user_id 컬럼 GRANT 제외로 소유권 이관 차단)·DELETE 차단, presets는 "읽기=인증 전체/쓰기=service_role" 패턴으로 봉쇄. `pg_class.relrowsecurity` 순회 메타 단정으로 신규 테이블의 7번째 재발을 근본 차단(과거 6건 모두 이 테스트가 있었으면 조기 발견됐음) |
| 2026-07-15 | F-AUTH-02 카카오/네이버 OAuth **설계 확정**(실 구현은 후속 라운드로 분리) — provider별 상이한 통합 경로(카카오=OIDC / 네이버=커스텀 브리지), (auth_provider, oauth_subject) 신뢰 소스·이메일 미제공 placeholder 정책, Flow-0-S(A-04/A-05 스킵·A-08 동의 즉시 INSERT), A-11 무화면 콜백, users 스키마 변경안, 소셜 버튼 컴포넌트 | `01-prd.md` §5-1-1(신규), `04-workflow.md` Flow-0-S(신규), `05-erd.md` §2-1, `02-ia.md` §3-2 A-11·각주, `03-uiux.md` §6-8(신규) | Supabase 네이티브 provider 미지원으로 provider별 통합 경로 비대칭 — 설계 검토 없이 구현 착수 시 카카오/네이버 방식 혼동 위험. role=person 셀프 가입 인바리언트(persons.id=primary_guardian_id=auth.uid()) 유지 명시 |
| 2026-07-15 | F-G-10 권한 자동 만료 배치(일 1회)·D-7 사전 알림·분기별 미사용 권한 감사 요약을 pg_cron+plpgsql로 구현(`05-erd.md` §4-5③⑤). 검토 중 `permission_logs.actor_id`가 cron 배치(SECURITY DEFINER, auth.uid() NULL) 경로에서 항상 NULL이 되어 사람 미개입과 구분 안 되는 결함 발견·`actor_type`('user'/'system') 컬럼으로 핫픽스 | 마이그레이션 `p3_permission_expiry_batch`, `p3_permission_logs_actor_type`, `supabase/prisma/schema.prisma` | 과다권한 요약(PRD 언급)은 판정 기준 미정의로 이번 범위 제외, 후속 작업. security-rls 에이전트는 사용량 한도로 실행 실패 — 메인 세션이 대신 체크리스트 검토 후 actor_type 핫픽스 직접 적용 |
| 2026-07-15 | F-G-10 정식 security-rls 검토(위 라운드 재검증) — **결함 없음** 결론 + pgTAP 15 신설(`supabase/tests/15_permission_expiry_batch.sql`, 15 asserts). REVOKE ALL FROM PUBLIC(authenticated 직접호출 차단)·D-7 주보호자 스코프·80일 재발송 창(분기 최소간격 90일>80 안전)·actor_type 'user'/'system' 분기·대량 UPDATE의 cache 무효화 트리거(pg_net 비동기 큐라 배치 트랜잭션 미차단) 전부 검증 | `supabase/tests/15_permission_expiry_batch.sql`(신규) | 이전 라운드가 사용량 한도로 비공식 수동검토에 그쳐 정식 재검토. 관찰(비결함, 미수정): ①D-7 사전알림은 `valid_until = today+7` 정확일치라 cron 다운타임 시 캐치업 없음(PRD "정확히 7일" 스펙과 합치) ②배치가 invalidate_permission_cache 를 행마다 발동→행당 vault 복호화 2회, 예상 규모(당사자 수백)에선 무해하나 대규모 시 statement-level/시크릿 캐시 검토 여지. 로컬 psql/pg_prove 미설치로 실행검증은 미수행(정적 검토) |
| 2026-07-17 | qa-verifier 전체 갭 분석 — CLAUDE.md "구현 현황"이 실제(Phase 0~3 대부분 진행)와 상충하던 것을 정정. 이어서 `docs/05-erd.md` ↔ `schema.prisma` ↔ 실 마이그레이션 컬럼 단위 대조 실시, 4건 문서 갭 보완: ① `notifications`/`notification_preferences.type`이 실제로는 text+CHECK(5값)가 아니라 Postgres ENUM(9값 — `life_stage_youth`/`life_stage_adult`/`permission_expiry_warning`/`permission_audit_summary` 4개 후속 추가분 누락 상태)이던 것을 §2-11/§2-11-1에 반영 ② `permission_logs.actor_type` 컬럼(P3-actor_type 핫픽스, 위 2026-07-15 항목)이 §2-5 SQL에서 누락돼 있던 것을 보완 ③ §2-1 users에 실제 구현된 `deactivated_at`(P2 privacy_settings) 누락 보완 ④ §7 "Prisma 스키마 참조"가 User/Person/Record 3개 모델만 담은 초기 스냅샷으로 방치되어 §2·실 스키마와 상충하던 것을 삭제하고 `schema.prisma`를 신뢰 소스로 지목하는 안내문으로 교체 | `CLAUDE.md`(구현 현황 섹션), `docs/05-erd.md` §2-1/§2-5/§2-11/§2-11-1/§7 | 코드베이스가 Phase 0~3까지 진척된 뒤에도 ERD가 P3 라운드(생애주기 알림·권한만료 배치·actor_type 핫픽스)의 스키마 변경을 따라잡지 못해 신뢰 소스 역할을 못 하고 있었음. web의 4개 전문가 역할(사회복지사·치료사·교사·활동지원사) 대시보드 라우팅 누락 의심은 재확인 결과 오탐으로 판명(`apps/web/src/app/(app)/home/page.tsx`가 역할별 분기 이미 구현) — qa-verifier의 화면 갭 판정은 라우트 디렉토리 존재 여부만으로는 오탐 가능성이 있어 추가 신뢰 검증 필요. 미완료 후속 항목: docs/02-ia.md 화면 인벤토리 1:1 매핑표, Redis 캐시 무효화 실패 시나리오 security-rls 재검토 |
| 2026-07-17 | `docs/02-ia.md` 화면 인벤토리 ↔ `apps/web`/`apps/mobile` 실제 라우트·화면 파일 1:1 매핑 검증. §3-1 요약표의 "웹 88 + 모바일 81"이 §3-2~3-8 상세 인벤토리(웹 45개 ID)와 근거가 맞지 않는 것을 발견해 각주로 정정 대기 표시. 웹 45개 ID 중 44개는 URL까지 정확히 일치(A-11 OAuth 콜백만 설계 단계로 미구현, 기존에 확인된 갭과 일치), W-14(ISP 점검)는 URL을 `/records/isp/:id` → `/records/isp/:id/review`로 정정. 문서에 없던 구현 완료 화면 3건에 ID 신규 부여(G-02 당사자 등록 위자드, A-07b 비밀번호 재설정 확인 — A-07은 재설정 "요청" 단계로 범위 좁힘) + `/settings` 인덱스는 단일 진입점이라 ID 미부여로 명시. `/attachments`는 P0-6 Storage 검증용 임시 테스트 페이지로 확인되어 IA 화면으로 카운트하지 않고 정식 UI 통합 후 제거 대상으로 각주 처리 | `docs/02-ia.md` §3-1/§3-2/§3-3/§3-7, §3-9 앞 신규 각주 | "88+81" 총계의 산출 근거는 이번 조사로도 확인 불가 — 문서 최초 작성 시점(2026-07-07)부터 세부 항목 나열 없이 총계만 기재된 것으로 추정. 후속 라운드에서 총계를 재산출하거나 "미상"으로 명시할지 결정 필요 |
| 2026-07-17 | §3-1 요약표를 "웹 88/모바일 81"(근거 미상) → §3-2~3-8 실제 ID를 role별로 직접 센 검증값(웹 합계 46, G-02·A-07b 신규분 포함)으로 교체. 모바일은 role별 화면 ID 목록이 이 문서에 아예 없어 재구성 불가 — `apps/mobile/src/screens` 파일 수(44개)만 참고치로 남기고 "미상" 표기, role별 목록화는 별도 후속 작업으로 이월. 이어서 security-rls에 Redis 권한 캐시 무효화 파이프라인(§4-5④) 재검토 요청 — **결함 없음** 결론: `apps/web/src` 전수 grep 결과 캐시 read 유틸리티(`getCachedPermission` 등)는 정의부만 있고 호출부가 전무해 "채택은 향후 라운드"라는 기존 서술이 사실과 일치함을 재확인, 무효화 트리거의 WHEN절·키 구성(OLD/NEW 양쪽 DEL)·Vault 미설정 시 no-op 가드도 SQL 원문과 일치 확인 | `docs/02-ia.md` §3-1, `docs/05-erd.md` §4-5④ 신규 각주 | 향후 캐시를 실제 read-path에 채택할 때 지킬 원칙 3가지를 §4-5④에 권고안으로 기록(캐시는 부가 최적화로만·RLS가 항상 최종 게이트, read 실패/미스는 DB 폴백, 채택 전 Vault 시크릿 스테이징 검증) — 이번 갭 분석 라운드(CLAUDE.md 구현 현황 정정 → ERD 컬럼 대조 → IA 화면 매핑 → Redis 캐시 재검토)로 사용자 요청 항목 전부 종결 |
| 2026-07-17 | 로컬 Supabase(Docker) 기동 + 밀려있던 마이그레이션 13건 적용 후, 역할별(당사자 포함 6역할) 테스트 계정 7개 + 당사자 3명(아동기·청소년전환기·성년기 각 1명) + `docs/05-erd.md` §3의 11개 record_type 전부 10건 이상(`supabase/seed.sql` 신설, 멱등 재실행 가능) 목업 데이터 생성. 이어서 §3-2 도메인 매트릭스를 record_type 단위로 대입한 §3-2-1 신설(`01-prd.md`) — RLS는 도메인 단위로만 강제하고 record_type 구분은 앱 레이어(폼+Zod) 책임임을 명시, 특수교사가 TRA write 프리셋 보유 시 DB상 TRA-001 작성이 막히지 않는 점(앱 미제공 경로)과 보호자의 구조적 전체 접근이 record_type을 구분 안 하는 점 2가지를 주의사항으로 기록 | `supabase/seed.sql`(신규), `docs/01-prd.md` §3-2-1(신규) | 개발 서버 빌드·기동 중 발견한 Base UI `Button`의 `nativeButton` 콘솔 경고(모든 `render={<Link>}` 호출부, 17개 파일)는 `apps/web/src/components/ui/button.tsx` 한 곳에서 `render` prop 존재 시 `nativeButton` 기본값을 `false`로 전환해 해결 — 개별 호출부 수정 불필요. 목업 계정 비밀번호는 전부 `Ongil1234!`(로컬 전용) — 7개 계정 전부 GoTrue 비밀번호 로그인 실제 검증(HTTP 200) 완료 |
| 2026-07-17 | **생애주기 3단계→5단계 확장 채택**(`07-lifecycle-record-permission-proposal.md` v0.2 정식 반영) — 영유아기(0~5)·아동기(6~12)·청소년전환기(13~18)·성인기(19~64)·노년기(65+). 성년 경계 18→19세 상향(민법 정합). DB: `get_life_stage()`·`assign_record_confirmer()`·`process_life_stage_transitions()` 갱신(`20260717000000_p3_lifecycle_5stage` 마이그레이션, `NotificationType`에 `life_stage_senior` 추가) + 로컬 적용·5단계 경계값 실측 검증 완료. 코드: `packages/shared/src/types.ts`(`Person.lifeStage` 5단계 유니언), `apps/web/src/lib/lifecycle.ts`·`apps/mobile/src/lib/iep.ts`에 `isSelfConfirmingStage`/`isPreTransitionStage` 헬퍼 신설 후 web 9개·mobile 8개 파일의 `=== "adult"`/`!== "child"` 인라인 비교(확인주체·전환계획 잠금 조건) 전량 치환, `StageBadge`(web/mobile) 아이콘·색상·설명 추가, 타임라인 단계 필터·구분선(6·13·19·65세) 갱신. 문서: `01-prd.md` §3-1, `05-erd.md` §2-2-1/§4-6②, `02-ia.md` §3-9 정식 반영. web 빌드·타입체크, mobile 타입체크 전부 통과 확인 | `supabase/prisma/migrations/20260717000000_p3_lifecycle_5stage/`, `supabase/prisma/schema.prisma`, `packages/shared/src/types.ts`, `apps/web/src/lib/lifecycle.ts`, `apps/mobile/src/lib/iep.ts`, `01-prd.md`·`05-erd.md`·`02-ia.md`·`07-lifecycle-record-permission-proposal.md` | SocialWorkerHome(web/mobile)의 "성인 서비스 전환 필요" 배지·정렬은 의도적으로 `adult` 단독 비교 유지(성인기 진입 시점 전용 알림이라 노년기 포함 시 의미가 달라짐) — 일괄 치환 대상에서 제외. PIPA 동의 화면의 "만 14세 이상입니다"(개인정보보호법 기준)는 life_stage와 무관한 별개 법정 기준이라 손대지 않음. 목업 데이터(seed.sql)의 3개 당사자는 나이 재계산해도 기존과 동일 단계로 분류돼(10/15/23세) 재시드 불필요. §07 문서의 LEG-001/002 신설 등 나머지 갭(③④⑥⑦)은 이번 라운드 범위 밖으로 남음 |
| 2026-07-17 | `07-lifecycle-record-permission-proposal.md` §5 잔여 갭 5건 중 4건(③④⑥⑦) + LEG-001/002 신설 라운드 완결. **LEG(법률·권리) 도메인**: `guardianshipReportSchema`(LEG-001, 후견감독보고서)·`advocacyConsultationSchema`(LEG-002, 권익옹호상담) 신설, `/records/leg`·`/records/leg/guardianship/new`·`/records/leg/advocacy/new` 3라우트 + `LegRecordsBoard`/`GuardianshipReportWizard`/`AdvocacyConsultationForm` 구현 → qa-verifier 검증 Match Rate 97%(`_workspace/05_qa_report.md`). **갭③ EDU-003 BIP**(행동중재계획, `bipSchema`) 신설, `/records/bip`·`/records/bip/new` + `BipForm` 구현. **갭④** DAI-002에 `scheduled_hours`(계획시간) 필드 추가(`service_hours`=실적시간과 의미 분리, 필드명 하위호환 유지). **갭⑥** SELF-001 보호자 대리 작성 — RLS는 이미 허용이었으나 앱 레이어(`submitSelfExpression`)가 person 역할만 강제해 막혀 있던 것을 발견, `persons/[id]/records/actions.ts`에 `createSelfExpressionForPerson` 신설 + `ProxyExpressWizard`(`/persons/[id]/records/express`) 구현. **갭⑦** 청소년기 TRA-001 특수교사 작성 — 백엔드/RLS는 이미 role-agnostic이었음(갭 아님 확인), `TeacherHome`에 `/records/transition/new` 진입점 추가로 해소. **보안**: security-rls 검토 중 SELF-001 대리 작성 경로에서 `records_insert` RLS가 author_id를 전혀 검사하지 않아 보호자가 `author_id=당사자id`로 위조해 대리 작성분을 "본인 작성(SELF-*)"으로 둔갑시킬 수 있는 결함 발견·즉시 핫픽스(`20260717120000_p3_records_author_id_antiforge`, author_id=auth.uid() pin) + `supabase/tests/02_records.sql`에 회귀 assert 추가(plan 21→22). **세션 재개 시 발견**: 이 마이그레이션이 라이브 DB에는 직접 SQL로 적용돼 있었으나 `_prisma_migrations` 이력에는 기록되지 않은 상태였음(security-rls가 psql로 직접 적용하고 `prisma migrate deploy`를 누락) — 재발견 후 `prisma migrate deploy` 재실행으로 이력 정합성 복구, 전체 20개 마이그레이션 스키마 재구축으로 재현성 확인 | `packages/validation/src/records.ts`, `apps/web/src/app/(app)/records/{bip,leg}/**`, `apps/web/src/app/(app)/persons/[id]/records/{actions.ts,express/page.tsx}`, `apps/web/src/app/(app)/journal/actions.ts`, `apps/web/src/components/{teacher/BipForm.tsx,social-worker/{LegRecordsBoard,GuardianshipReportWizard,AdvocacyConsultationForm}.tsx,guardian/ProxyExpressWizard.tsx,teacher/TeacherHome.tsx}`, `supabase/prisma/migrations/20260717120000_p3_records_author_id_antiforge/`, `supabase/tests/02_records.sql`, `docs/05-erd.md` §3(EDU-003/LEG-001/002/DAI-002 갱신) | `pnpm typecheck`(validation·web·mobile)·`pnpm --filter @ongil/web build` 전부 통과, 신규 라우트 6개 전부 ƒ(dynamic) 등록 확인. pgTAP 전체 스위트(`supabase test db`)는 대부분 파일이 "duplicate key"/"bad plan" 오류로 실패하는데, 이는 2026-07-15 항목에 이미 기록된 "로컬 pg_prove 미설치" 계열의 기존 실행 환경 한계(각 테스트 파일이 격리되지 않고 상태를 공유)이며 이번 변경으로 인한 회귀가 아님(스키마를 완전히 지우고 마이그레이션만 재적용한 무-seed 상태에서도 동일하게 재현) — 02_records.sql 자체도 이 하네스 결함으로 실행 실패해 신규 assert의 실제 실행 검증은 못했고 정책 SQL을 라이브 DB에서 직접 조회해 대조하는 방식으로 대체 검증함. `07_invitations.sql`은 9~11번 subtest가 실제로 실패(pre-existing, 이번 라운드 무관, 별도 확인 필요). pgTAP 하네스 안정화(각 파일 독립 트랜잭션 보장)는 별도 후속 작업으로 백로그 등록 권고 |
| 2026-07-17 | 생애주기×이해관계자×분야×기록유형 권한 매트릭스를 Artifact로 정리한 뒤, 이를 기준으로 qa-verifier에게 설계-구현 갭 분석 위임(4축: 생애주기별 화면 가드·확인주체 5단계 정합·역할×record_type RLS 일치·GEN-001 안전성) — 종합 Match Rate 92%. **P1 갭 발견·즉시 수정**: LEG-001/002(`records/leg/actions.ts`)가 TRA-001과 달리 연령 가드가 전혀 없어 영유아기 당사자에게도 후견감독보고서·권익옹호상담 작성이 가능했음(docs/07 §4-4/§4-5는 "성인기·노년기(19세+)부터"로 명시하는데 코드는 role+RLS만 확인). `createGuardianshipReport`/`createAdvocacyConsultation`에 `isSelfConfirmingStage` 서버 재검증 추가(TRA-001의 `isPreTransitionStage` 패턴과 동형), `GuardianshipReportWizard`/`AdvocacyConsultationForm`에도 동일한 차단 배너·버튼 비활성화 추가. 나머지는 결함 아님으로 확인: 확인주체 배정(축2)·GEN-001 안전성(축4)은 100%, EDU-003(BIP) 연령 무가드는 설계상 "자연 감소"라 하드블록 대상이 아니므로 갭 아님. P3 2건(목록 쿼리 `valid_until` 미필터 — records 전체 화면 공통의 기존 관행이라 3개 파일만 부분 수정하지 않고 보류, 노년기 EDU 비현실적 조합)은 재현 시나리오가 비현실적이거나 프로젝트 전역 관행이라 이번 라운드에서 손대지 않음 | `apps/web/src/app/(app)/records/leg/actions.ts`, `apps/web/src/components/social-worker/{GuardianshipReportWizard,AdvocacyConsultationForm}.tsx`, `docs/05-erd.md`(LEG-001/002 절 연령가드 각주) | `pnpm --filter @ongil/web typecheck`·`build` 통과 확인. qa-verifier 리포트는 `_workspace/08_qa_lifecycle_matrix.md` 참조 |
| 2026-07-17 | 온길 권한 매트릭스 Artifact에 현장 조사 기반 §08(설계 vs 현장 실무 비교, 14종 전수 대조)·§09(신규 반영 후보 6종) 섹션 추가 → 이를 입력으로 PM팀(PM·PL·SW시니어개발자·UIUX전문가·DB전문가)과 이해관계자(당사자·보호자·특수교사·활동지원사·사회복지사) 합동 협의를 시뮬레이션해 `docs/08-record-taxonomy-workshop.md` 신설. 후보 6건 중 **2건만 신규 record_type으로 채택**(EDU-005 개별화전환계획·WEL-006 사례회의록), 2건은 기존 유형에 필드만 병합(EDU-004 FBA→EDU-003, LEG-003 재산목록보고서→LEG-001 `report_kind`), 1건 기각(MED-008, 스코프 밖), 1건 별도 트랙 분리(학대의심 신고 에스컬레이션, 법률자문 선행). WEL-004/005 병합 여부·TRA-001/EDU-005 주체 분리 여부도 재확인해 **분리 유지**로 결론(갱신 주기 불일치·작성주체 불일치가 근거) | `docs/08-record-taxonomy-workshop.md`(신규), `CLAUDE.md`(핵심 문서 표에 07/08 추가) | 최종 record_type 16종(기존 14+신규 2) 확정은 이 문서의 §5 표가 단일 소스. 실제 구현은 아직 착수 전 — §6 후속조치에 우선순위(P2/P3/P4/별도트랙) 정리됨, 다음 요청 시 EDU-005부터 `ongil-dev`로 착수 권장 |
| 2026-07-17 | `docs/08-record-taxonomy-workshop.md` §6 후속조치를 단순 목록에서 **실행 계획**으로 재정리 — Wave 1(필드 병합 2건·UX 연결 강화·스코프 각주·프론트 전용 백로그 2건, 마이그레이션 불필요·즉시 착수 가능)/Wave 2(신규 record_type 2건, WEL-006→EDU-005 순서로 "간단한 것 먼저" 원칙 적용)/Wave 3(F-LEG-11, 법률자문 선행 전까지 미착수)로 웨이브 구분하고 항목별 담당 순서·의존성·완료조건 명시. EDU-005 설계 시 LEG 라운드에서 겪은 "연령가드 누락" 실수(2026-07-17 이전 항목의 P1 갭) 재발 방지 체크리스트 항목으로 명문화 | `docs/08-record-taxonomy-workshop.md` §6 | 다음 작업 요청 시 "Wave 1 전체" 또는 "WEL-006"부터 `ongil-dev`로 바로 착수 가능한 상태 |
| 2026-07-17 | `docs/01-prd.md`에 §3-2-2 "기록유형 카탈로그" 신설 — 14개 record_type 전체를 분야·유형·기록명·기록설명·주작성자·보조작성자·열람가능자 7열로 정리(먼저 Artifact로 만든 매트릭스의 ③기록유형 카탈로그 섹션을 문서에 반영). §3-2-1에 LEG-001/002 연령 가드(2026-07-17 수정) 각주도 추가 | `docs/01-prd.md` §3-2-2(신규), §3-2-1 주의사항 3번(신규) | 보조작성자가 존재하는 유형은 SELF-001·TRA-001 2개뿐임을 명시적으로 정리 — 나머지 12개는 단일 역할 전담 작성 |
| 2026-07-17 | `docs/08-record-taxonomy-workshop.md` §6 Wave 1(6개 항목) 구현 완료. EDU-003에 `fba_basis`(기능평가 근거, 복수선택) 필드·`BipForm` 체크박스 그룹 추가, LEG-001에 `report_kind`('initial'/'periodic', 기본값 periodic) 필드·`GuardianshipReportWizard` 드롭다운·`LegRecordsBoard` "최초 보고" 배지 추가(둘 다 신규 record_type 없이 필드 병합, 마이그레이션 불필요). ISP 점검 화면(`IspReviewPane`)에 서비스 현황(WEL-005) 진입 링크 추가 — 장식용 링크에 그치지 않도록 `/records/service-status`가 `?personId=` 쿼리를 실제로 받아 `ServiceUsageTable`이 해당 당사자로 필터링("전체 보기" 해제 가능)하도록 함께 구현. `docs/01-prd.md`에 "신청·자격심사 단계는 다루지 않는다" 스코프 각주 추가. `JournalWizard`(활동지원일지)는 2단계 진입 시 이전 일지를 자동으로 미리 불러오도록 변경(기존엔 수동 클릭 필요) — 카테고리가 이미 고정 6종 토글이라 "자동완성"보다 이 방식이 더 실효성 있다고 판단해 계획을 변경. `ExpressWizard`/`ProxyExpressWizard`(자기표현)는 `memo` 입력을 "메모 남기기" 클릭 없이 상시 노출로 변경 — `voice_url`은 녹음 UI 자체가 없어(스키마만 존재) 이번 라운드 범위 밖으로 남김 | `packages/validation/src/records.ts`, `apps/web/src/components/teacher/BipForm.tsx`, `apps/web/src/components/social-worker/{GuardianshipReportWizard,LegRecordsBoard,IspReviewPane,ServiceUsageTable}.tsx`, `apps/web/src/app/(app)/records/leg/actions.ts`, `apps/web/src/app/(app)/records/service-status/page.tsx`, `apps/web/src/components/supporter/JournalWizard.tsx`, `apps/web/src/components/{person/ExpressWizard,guardian/ProxyExpressWizard}.tsx`, `docs/01-prd.md`(스코프 각주), `docs/05-erd.md`(EDU-003/LEG-001 필드 각주), `docs/08-record-taxonomy-workshop.md` §6(완료 표시) | `pnpm typecheck`(validation·web)·`pnpm --filter @ongil/web build` 전부 통과. qa-verifier 정식 검증은 계획대로 생략(가벼운 변경). 다음 착수 대상은 Wave 2-1 WEL-006(사례회의록) |
| 2026-07-17 | `docs/08-record-taxonomy-workshop.md` §6 Wave 2-1 **WEL-006(사례회의록) 신규 구현** 완료 — 2026-07-17 워크숍이 채택한 신규 record_type 2건 중 첫 번째. `packages/validation`에 `caseConferenceNoteSchema`(meetingDate·participants·discussion·decisions, requires_confirmation=false, camelCase — EDU-002/LEG-002 계열 일상기록 컨벤션) 신설. 서버 액션 `apps/web/src/app/(app)/records/case-notes/actions.ts`(getCaseNoteClients/listCaseNotes/createCaseConferenceNote, LEG 라운드와 동일 구조) + 컴포넌트 `CaseNotesBoard`(목록)·`CaseConferenceForm`(단일 폼, 비위자드) + 라우트 `/records/case-notes`·`/records/case-notes/new`(IA `W-22`/`W-23` 신규 부여) 구현. 저장 시 확인 절차 대신 당사자·보호자에게 `record_new` 알림만 best-effort 발송(guardians 테이블 조회 + person 셀프계정, 실패해도 저장 유지). `SocialWorkerHome`·`IspReviewPane`(ISP 점검 화면)에 진입점 추가. **RLS 변경 없음**을 라이브 DB에서 직접 검증 — `records_select/insert/update` 정책 SQL을 재조회해 `domain`·`access_level`만 참조하고 `record_type`을 전혀 보지 않음을 재확인, 워크숍의 "신규 record_type도 도메인 단위 RLS가 그대로 커버할 것"이라는 예측이 실제로 맞았음을 확인 | `packages/validation/src/records.ts`, `apps/web/src/app/(app)/records/case-notes/{actions.ts,page.tsx,new/page.tsx}`(신규), `apps/web/src/components/social-worker/{CaseNotesBoard,CaseConferenceForm}.tsx`(신규), `apps/web/src/components/social-worker/{SocialWorkerHome,IspReviewPane}.tsx`, `docs/05-erd.md`(WEL-006 §3 신설), `docs/01-prd.md`(§3-2-1/§3-2-2 WEL-006 행, 유형 수 14→15), `docs/02-ia.md`(W-22/23 신규, 웹 화면 합계 46→48), `docs/08-record-taxonomy-workshop.md` §6(Wave2-1 완료 표시) | `pnpm typecheck`(validation·web)·`pnpm --filter @ongil/web build` 전부 통과, `/records/case-notes`·`/records/case-notes/new` 라우트 정상 등록 확인. qa-verifier 정식 검증은 계획대로 간이 검증(라이브 RLS 정책 대조)으로 대체 — 가벼운 신규 기능이라 LEG 라운드 수준의 전체 Match Rate 측정은 생략. 다음 착수 대상은 Wave 2-2 EDU-005(개별화전환계획) |
| 2026-07-17 | `docs/08-record-taxonomy-workshop.md` §6 Wave 2-2 **EDU-005(개별화전환계획 ITP) 신규 구현** 완료 — 워크숍이 채택한 신규 record_type 2건 중 두 번째이자 Wave 2 전체 마무리. `packages/validation`에 `itpSchema`(career_interest_areas·work_experience_log·next_step_note·next_review_date, requires_confirmation=true, snake_case — EDU-001/003 계열 공식문서 컨벤션), `apps/web/src/lib/lifecycle.ts`에 `isItpActiveStage`(청소년 전환기만 true, TRA-001의 `isPreTransitionStage`와 활성 구간이 다름에 주의) 신설. 서버 액션 `records/itp/actions.ts`(getItpClients/createItp/listItpRecords/getItpDetail/getLatestItpSummary) + 4단계 위자드 `ItpWizard.tsx`(TransitionPlanWizard 모델) + 라우트 `/records/itp`·`/records/itp/new`(IA T-19 신규) + `TeacherHome` 조건부 진입점 + `TransitionPlanWizard`에 `getLatestItpSummary` 기반 소프트 링크 참고 패널(FK 없음, person_id만). **연령 가드는 LEG 라운드의 실수를 반복하지 않고 서버·UI 양쪽에 정확히 구현됨**(qa-verifier 확인) — **그러나 검증 중 훨씬 크고 광범위한 선재 결함을 발견**: `persons_select` RLS가 permissions 보유 전문가 분기를 아예 갖고 있지 않아 ①BIP·LEG·ISP·TRA 등 기존 6개 record_type 전부의 "담당 당사자 목록" 조회가 전문가에게 항상 빈 목록으로 보였고 ②`assign_record_confirmer` 트리거(SECURITY INVOKER)가 전문가 작성 기록의 confirmer_id를 계속 NULL로 남겨 확인요청 알림이 발송되지 않고 있었음(보호자 작성분만 우연히 정상). 로컬 Supabase에서 teacher1 계정으로 `SET LOCAL role authenticated`+JWT claim 시뮬레이션해 실측 재현(0 rows) 후 즉시 수정 — `supabase/prisma/migrations/20260717130000_p3_persons_select_permission_holders/`(records_select와 동형의 permissions 분기 추가), 재현 검증으로 teacher1의 persons SELECT(0→2 rows)·EDU-005 INSERT 시 confirmer_id 정상 세팅 확인, `supabase/tests/01_persons.sql`에 회귀 assert 추가(plan 9→10, 클린 스키마 단독 실행으로 신규 assert "ok" 확인). qa-verifier 1차 검증 Match Rate 72%(이 RLS 갭 때문 — 갭 자체는 EDU-005 코드 결함이 아니라 선재 인프라 결함). 문서 미반영 갭(IA T-19·ERD EDU-005 §3·PRD 매트릭스 전부 누락돼 있었음)도 발견 즉시 보완 | `packages/validation/src/records.ts`, `apps/web/src/lib/lifecycle.ts`, `apps/web/src/app/(app)/records/itp/{actions.ts,page.tsx,new/page.tsx}`(신규), `apps/web/src/components/teacher/{ItpWizard,TeacherHome}.tsx`, `apps/web/src/components/social-worker/TransitionPlanWizard.tsx`, `supabase/prisma/migrations/20260717130000_p3_persons_select_permission_holders/`(신규), `supabase/tests/01_persons.sql`, `docs/05-erd.md`(EDU-005 §3 신설 + §4-1 persons_select 정정이력), `docs/01-prd.md`(§3-2-1/§3-2-2 EDU-005 행, 유형 수 15→16), `docs/02-ia.md`(T-19 신규, 웹 화면 합계 48→49), `docs/08-record-taxonomy-workshop.md` §6/§7(신규, RLS 갭 상세) | `pnpm typecheck`(validation·web)·`pnpm --filter @ongil/web build` 전부 통과. **후속 권고**: 이 RLS 갭이 EDU-005 하나만의 문제가 아니었으므로, 이미 배포된 BIP·IEP·ISP·LEG·TRA·WEL-006의 담당 당사자 목록 화면이 실제 프로덕션에서도 정상 동작하는지(개발 중엔 seed 데이터를 superuser 경로로 만들어 눈치채기 어려웠을 가능성) 실 사용자 세션 스팟체크 권장. pgTAP 하네스의 fixture 이메일 충돌(01_persons.sql, uuid 앞 8자리 중복 시 자동생성 이메일 충돌) 버그도 이번에 처음 발견 — 별도 백로그. Wave 1·2 전부 완료, 남은 항목은 Wave 3(F-LEG-11, 법률자문 선행 대기) |
| 2026-07-18 | `docs/08-record-taxonomy-workshop.md` §7 후속 권고("BIP·IEP·ISP·LEG·TRA·WEL-006 담당 당사자 목록이 실제로 정상 동작하는지 스팟체크") 이행 완료 — `persons_select` RLS 수정(전날 `p3_persons_select_permission_holders`)이 EDU-005 하나가 아니라 전체 전문가 역할에 실제로 적용되는지 5개 역할(teacher1/therapist1/supporter1/social1×2도메인) × 5개 record_type(EDU-005/MED-005/WEL-004/TRA-001/LEG-001)으로 실측. `SET LOCAL role authenticated` + JWT claim 시뮬레이션으로 각 역할의 persons SELECT(전부 2 rows)와 confirmer_id 세팅(전부 담당 보호자로 정상)을 확인. LEG는 social1에게 시드에 없던 권한이라 트랜잭션 내에서 임시 부여 후 검증·롤백(시드 데이터 무변경) | `docs/08-record-taxonomy-workshop.md` §7(스팟체크 결과 표 추가) | 추가 코드 변경 없음(순수 검증) — persons_select 수정 하나로 6개 기존 기능 + 신규 2개(WEL-006·EDU-005) 전부가 동시에 고쳐졌음을 확정. Wave 1·2·후속권고까지 이번 record 체계 재설계 워크숍의 전 항목이 종결됨 |
| 2026-07-18 | 온길 권한 매트릭스 Artifact를 최신 설계로 전면 갱신(§03 신규: 생애주기×기록×이해관계자 전체 권한표 — 5개 생애주기 아코디언, 각 단계 활성 record_type×6역할 접근수준×확인주체 교차표; §04~10 renumbering 및 WEL-006·EDU-005 반영; §08에 persons_select RLS 갭 요약 추가; §10 신규 후보 6건 전부에 "결론" 열 추가해 채택 완료 표시) 후, 동일 내용을 `docs/08-record-taxonomy-workshop.md` §8(신규)로 마크다운 5개 표 형태로 문서화 | 온길 권한 매트릭스 Artifact(같은 URL 유지), `docs/08-record-taxonomy-workshop.md` §8(신규)·헤더 메타 갱신 | Artifact와 workshop 문서가 동일한 생애주기×기록×이해관계자 권한표를 각각 인터랙티브 아코디언(Artifact)·정적 마크다운 표(workshop 문서) 형태로 보유 — 둘 다 GEN-001은 생애주기 무관 불변이라 표에서 생략하고 카탈로그로 안내하는 동일한 설계 결정을 공유 |
| 2026-07-18 | `docs/01-prd.md`에 §3-2-3 "기록유형별 서식(전체 필드 스키마)" 신설 — 16개 record_type 전체의 `content` JSONB 필드를 필드명·타입·필수여부·설명까지 표로 정리(온길 기록 서식 전체 목록 Artifact와 동일 내용을 문서화, `packages/validation/src/records.ts` 원문 기준). §3-2-2 intro의 "14개" 표기를 "16개"로 정정, content 스키마 참조를 `docs/05-erd.md` §3에서 신규 §3-2-3으로 변경 | `docs/01-prd.md` §3-2-3(신규), §3-2-2 intro 정정 | `records.ts`가 신뢰 소스이고 이 표는 그 스냅샷이라는 점을 문서 말미에 명시 — 향후 스키마 변경 시 이 표도 함께 갱신 필요 |
| 2026-07-18 | qa-verifier에게 전체 설계문서(01~08) 종합 정합성 검토 위임 — Match Rate 93%, P1(코드-문서 불일치) 0건 확인(record_type 16종 카운트·PRD §3-2-3 서식표·ERD §3 스키마·IA 라우트 전부 실제 코드와 1:1 일치). P2 2건 발견 즉시 수정: ① `docs/02-ia.md` §3-1 요약표가 총계만 갱신되고 역할별 행이 stale이었던 반복 실수(특수교사 6→실제 9, 사회복지사 8→실제 11 등) — 이번엔 §3-2~3-8 전 구간을 라인 범위별로 재검증해 역할별 행+합계(57) 동시 수정 ② `docs/07-lifecycle-record-permission-proposal.md` §5의 v0.1 갭 7건 해소 여부 표가 2026-07-09 시점에 멈춰 있어 이후 라운드(LEG/BIP 신설, TeacherHome 진입점)의 실제 해소를 반영 못함 — "이후 처리 상태(2026-07-18)" 열 추가해 ①③④⑤⑦ 해소·⑥만 미해소로 갱신. P3 1건도 함께 수정: `docs/05-erd.md` §4-6① 확인절차 기본값 가이드에서 LEG 도메인 전체가 누락돼 있던 것을 EDU-005·WEL-006·LEG-001/002까지 포함한 명시적 목록으로 교체(기존 `EDU-*` 와일드카드 표기가 EDU-002를 오분류할 수 있는 모호함도 함께 해소) | `docs/02-ia.md` §3-1, `docs/07-lifecycle-record-permission-proposal.md` §5, `docs/05-erd.md` §4-6① | qa-verifier 리포트는 `_workspace/10_qa_all_docs_review.md` 참조. `docs/03-uiux.md`·`docs/04-workflow.md`·`docs/06-wbs.md`는 EDU-005/WEL-006 미반영이 확인됐으나 이 문서들이 애초에 record_type 세부를 다루지 않는 성격이라 범위 밖으로 판정(갭 아님) |
| 2026-07-18 | `docs/09-cto-team-gap-analysis.md` 신설 — CTO팀(PM·PL·아키텍처·DB·SW시니어·UIUX·프론트엔드·백엔드·보안·QA 10인) 시뮬레이션으로 설계·현재 개발내용 전 항목·분야별 갭분석 수행. 실측 근거: 마이그레이션 22개·pgTAP 18개·웹 라우트 52개·모바일 화면 45개 grep 대조. 8건 발견 — **P2**: 모바일 파리티 완전 결여(신규 5종 record_type 전부 미이식, 실측 0건 확인)/신규 5종에 pgTAP 회귀 테스트 전무/pgTAP 하네스 fixture 이메일 충돌 미해결. **P3**: 정책 교차검증 메타테스트 부재/알림 발송 로직 3곳(handovers·eval·case-notes) 중복. **관찰(비결함)**: person_id 소프트링크·getXxxClients 골격 반복은 "질서있는 패턴 재사용"으로 CTO팀이 지금 리팩토링하지 않기로 합의(YAGNI, 4번째 사례 시 재논의). 새 P1 결함 없음. CTO팀 회의에서 우선순위·의존관계 협의 후 Wave A(알림유틸 통합, 즉시)→Wave B(pgTAP 하네스 수정→회귀테스트→메타테스트)→Wave C(모바일 파리티 2단계: BIP·ITP 우선, WEL-006·LEG 후속)→Wave D(E2E 스팟체크·OAuth·학대신고는 별도 트랙) 작업계획서로 확정 | `docs/09-cto-team-gap-analysis.md`(신규), `CLAUDE.md`(핵심 문서 표) | 문서 작성만 완료, 실제 구현은 미착수 — 다음 요청 시 Wave A(알림 유틸 통합)부터 `ongil-dev`로 착수 가능 |
| 2026-07-18 | `docs/09-cto-team-gap-analysis.md` Wave A(알림 발송 공통 유틸 추출) 완료 — `apps/web/src/lib/notify.ts` 신설(`notifyRecipients`, `logAccess`와 동일하게 자체 Supabase client 생성 + best-effort try/catch 컨벤션, `NotificationType` 로컬 타입 정의로 문자열 리터럴 오타 방지). `handovers/actions.ts`·`records/eval/actions.ts`·`records/case-notes/actions.ts` 3곳에 중복돼 있던 "recipientIds 계산 → notifications.insert → try/catch 무시" 골격 중 insert+try/catch 부분만 공통 유틸 호출로 교체 — recipientIds 산출 로직(기능마다 다름: 인수인계 단일 수신자, 평가보고서는 보호자+담당 사회복지사 조회, 사례회의록은 guardians+본인 조회)은 그대로 유지해 순수 리팩토링으로 한정 | `apps/web/src/lib/notify.ts`(신규), `apps/web/src/app/(app)/{handovers,records/eval,records/case-notes}/actions.ts`, `docs/09-cto-team-gap-analysis.md`(Wave A 완료 표시) | `pnpm typecheck`·`pnpm --filter @ongil/web build` 통과, `grep`으로 `notifications` 직접 insert 호출이 `notify.ts` 한 곳만 남았음을 확인. 다음 착수 대상은 Wave B-1(pgTAP fixture 이메일 충돌 수정) |
| 2026-07-18 | Wave B-1(pgTAP fixture 이메일 충돌 수정) 완료 — `tests.mk_user()`를 8자리 UUID 프리픽스 대신 전체 UUID로 이메일 생성하도록 수정, 이 프로젝트 최초로 pgTAP 18개 파일 전체 완주 성공. 완주 과정에서 이전엔 도달 불가능했던 코드 경로의 결함 4건 연쇄 발견·전부 수정: ① 8개 파일·18개 지점의 중첩 데이터수정 CTE 구문 오류(`WITH ... AS (UPDATE/DELETE ...)`가 `SELECT is()` 인자로 중첩된 잘못된 구조) ② `log_permission_change()` 트리거가 `permissions` UPDATE(권한 회수·수정)마다, 그리고 F-G-10 자동 만료 배치 실행 시마다 2026-07-15부터 3일간 실패해 온 실제 프로덕션 버그(CASE 표현식 text→enum 암시적 캐스트 불가) — `p3_permission_logs_action_cast_fix` 마이그레이션으로 수정 ③ `04_audit_logs.sql`의 seed.sql 데이터 오염으로 인한 테스트 격리 결함 ④ **`users` 테이블 컬럼 GRANT 권한 상승 취약점(최고 위험도)** — `p0_4_rls_grants`가 이미 `users`에 테이블 단위 UPDATE를 전 컬럼 부여했는데 이후 `p2_privacy_settings`가 선행 `REVOKE` 없이 `GRANT UPDATE (deactivated_at)`만 추가(컬럼 권한은 가산적이라 no-op) — 임의 인증 사용자가 자기 `role`을 마음대로 변경(예: teacher→social_worker)해 권한 상승 가능한 상태였음을 라이브 세션에서 실제 재현·확인(롤백, 영구 반영 없음). `p3_users_column_grant_hotfix` 마이그레이션으로 `REVOKE UPDATE ON users FROM authenticated` 후 `deactivated_at`만 재부여해 차단, 재시도 익스플로잇은 `permission denied`로 정상 차단됨을 확인 | `supabase/tests/00_helpers.sql`, `supabase/tests/{02,03,04,05,08,09,12,16}_*.sql`(CTE 구문), `supabase/prisma/migrations/20260718000000_p3_permission_logs_action_cast_fix/`(신규), `supabase/prisma/migrations/20260718010000_p3_users_column_grant_hotfix/`(신규), `docs/09-cto-team-gap-analysis.md`(B-1 완료 상세) | ④는 이 세션에서 발견된 것 중 가장 심각한 결함 — RLS 자체는 켜져 있었으나 "컬럼 GRANT는 REVOKE 후 GRANT 순서가 필수"라는 원칙(`handover_notes`/`notifications` 핫픽스에선 이미 지켜졌던 패턴)을 `users`에서만 놓친 사례. 향후 컬럼 단위 제한 작업 시 반드시 REVOKE→GRANT 순서 검증할 것. `prisma migrate deploy`로 23개 마이그레이션 전부 적용, `docker exec`+`psql -f`로 18개 테스트 파일 개별 재실행해 전 파일 `not ok`/`ERROR` 0건 확인(기존에 알려졌던 `07_invitations.sql`·`10_life_stage_transitions.sql` 실패도 픽스처 버그의 부수효과였을 뿐 이번 수정으로 해소). 다음 착수 대상은 Wave B-2(신규 5종 record_type 전용 pgTAP 테스트) |
| 2026-07-18 | Wave B-2(EDU-003·EDU-005·WEL-006·LEG-001·LEG-002 전용 pgTAP 테스트) 완료 — `supabase/tests/18~22`(파일당 1 record_type) 신설, 각각 도메인 write 권한자 INSERT 가능·무권한자 INSERT 차단(RLS)·`requires_confirmation` 값에 따른 confirmer 자동 배정(또는 미배정)을 검증. EDU-005·LEG-001·LEG-002(앱 레이어 생애주기 가드 보유)는 "대상 연령대 밖 당사자에게도 DB 레벨 INSERT가 허용되고 confirmer는 실제 생애주기 기준으로만 배정됨"을 명시적으로 고정하는 경계 테스트를 추가 — records_insert RLS·`assign_record_confirmer()` 트리거 모두 record_type/생애주기 대상 적합성을 모른다는 기존 설계(연령가드=앱 레이어 전용)를 회귀 없이 문서화한 것. EDU-003·WEL-006은 애초에 연령가드가 없어 해당 시나리오 생략. 작성 중 테스트 자체의 버그 1건 발견·수정(파일 19·21의 경계 확인 INSERT가 `requires_confirmation`을 명시하지 않아 기본값 false로 삽입되면서 confirmer 트리거가 발동하지 않아 assert 실패 — 컬럼 명시로 수정, 실제 트리거/RLS 결함 아님). `docker exec`+`psql -f`로 신규 5개 파일 및 전체 23개 파일 재실행 모두 `not ok`/`ERROR` 0건 확인 | `supabase/tests/{18_bip_edu003,19_itp_edu005,20_case_conference_notes_wel006,21_guardianship_report_leg001,22_advocacy_consultation_leg002}.sql`(신규), `docs/09-cto-team-gap-analysis.md`(B-2 완료 상세) | Wave B-2 완료로 이 5개 record_type도 이제 pgTAP 회귀 안전망을 갖춤(이전엔 회귀 테스트 전무). 다음 착수 대상은 Wave B-3(정책 교차 검증 메타 테스트, persons_select ↔ records_select 논리 일관성) |
| 2026-07-18 | Wave B-3(정책 교차 검증 메타 테스트, persons_select ↔ records_select 논리 일관성) 완료로 **Wave B(B-1/B-2/B-3) 전체 종결** — `supabase/tests/23_persons_records_select_consistency.sql`(신규, plan 10) 신설. 2026-07-17 EDU-005 검증 중 발견됐던 persons_select permissions 보유자 분기 누락 사건(당시 개별 record_type 단위로만 재현·수정됨)을 "두 정책의 논리적 관계"라는 상위 층위에서 재발 방지하기 위한 메타 테스트 — 핵심 불변식은 "records_select가 보이면 persons_select도 반드시 보인다(단방향 함의)"이며, records_select의 permissions 분기만 domain을 요구하고 persons_select는 도메인 무관이라 "완전 동치"가 아님을 함께 고정(의도된 비대칭 1건 포함). 5개 시나리오(permissions 보유자·주보호자·비주 공동보호자·셀프 당사자·무권한자) × persons/records 양쪽 SELECT 검증. 전체 24개 pgTAP 파일 재실행 `not ok`/`ERROR` 0건 확인 | `supabase/tests/23_persons_records_select_consistency.sql`(신규), `docs/09-cto-team-gap-analysis.md`(B-3 완료 상세, Wave B 전체 완료 표시) | Wave B 전체 완료로 이제 신규 record_type 추가 시 회귀 테스트(B-2 패턴)뿐 아니라 persons/records 정책 간 논리 일관성(B-3 메타 테스트)까지 안전망이 갖춰짐. 다음 착수 대상은 Wave C-1(EDU-003·EDU-005 모바일 이식) |
| 2026-07-18 | Wave C-1(EDU-003 BIP·EDU-005 ITP 모바일 이식) 완료 — 웹 `bip/actions.ts`·`itp/actions.ts`·`BipForm.tsx`·`ItpWizard.tsx`를 모바일에 1:1 이식. `lib/iep.ts`에 `isItpActiveStage` 헬퍼 추가, `lib/bip.ts`·`lib/itp.ts` 신설(각 도메인 스코핑 쿼리·자체 `logAccess` 복제 관행 유지), `createItp`는 웹과 동일하게 `persons.birth_date` 서버 재조회 기반 연령가드 이중 검증 구현. `BipFormScreen.tsx`(단일 폼, `ObservationFormScreen` 패턴)·`ItpWizardScreen.tsx`(4단계 위자드, `TransitionPlanWizardScreen`의 연령가드 카드 관용구 재사용) 신설, 네비게이션 타입·`MainNavigator` teacher 스택에 등록, `TeacherHomeScreen`에 퀵액션 진입점 추가(ITP는 대상 학생 존재 시에만 노출). `pnpm --filter @ongil/mobile typecheck` 통과. **진행 경위**: 최초 구현을 맡은 mobile-dev 에이전트가 월간 사용량 한도로 `ItpWizardScreen.tsx` 작성 도중 중단됐으나 확인 결과 lib 2개·화면 2개·헬퍼·네비게이션 타입은 이미 완성돼 있었음(손실 없음) — 메인 세션이 네비게이터 등록·홈 화면 진입점·typecheck 마무리를 직접 완료 | `apps/mobile/src/lib/{iep,bip,itp}.ts`, `apps/mobile/src/screens/{BipFormScreen,ItpWizardScreen,TeacherHomeScreen}.tsx`, `apps/mobile/src/navigation/{types,MainNavigator}.tsx`, `docs/09-cto-team-gap-analysis.md`(C-1 완료 상세) | 모바일 에뮬레이터가 이 환경에 없어 시각적 확인(렌더링·터치 동작)은 미수행 — typecheck 통과로만 검증. 다음 착수 대상은 Wave C-2(WEL-006·LEG-001/002 모바일 이식, 이미 백그라운드 리서치 완료 상태) |
| 2026-07-18 | Wave C-2(WEL-006·LEG-001·LEG-002 모바일 이식) 완료 — **Wave C 전체 종결**. 웹 `case-notes/actions.ts`·`leg/actions.ts`·`CaseConferenceForm.tsx`·`GuardianshipReportWizard.tsx`·`AdvocacyConsultationForm.tsx`를 모바일에 1:1 이식. `lib/case-notes.ts`(신규, 알림은 `lib/handover.ts` 관행대로 호출부 직접 try/catch insert)·`lib/leg.ts`(신규, `assertSocialWorker` 역할 재확인 + `isSelfConfirmingStage` 서버 연령가드 재검증 이중 구현 — 2026-07-17 웹 라운드의 "연령가드 누락" 결함과 동형 재발 방지) 신설. `CaseConferenceFormScreen`(단일 폼)·`GuardianshipReportWizardScreen`(4단계, TRA-001과 반대 방향의 성인기 미만 차단 가드)·`AdvocacyConsultationFormScreen`(단일 폼) 신설 + 브리핑 범위 밖으로 자체 판단해 추가한 `LegBoardScreen`(웹 `LegRecordsBoard` 대응 LEG 허브, 이미 필요한 lib 함수가 갖춰져 저비용). 네비게이션 타입·`MainNavigator` social_worker 스택 등록, `SocialWorkerHomeScreen`에 진입점 2개 추가(항상 노출, 웹과 동일). datetime-local 미지원으로 텍스트 입력 대체(기존 모바일 관행). `pnpm --filter @ongil/mobile typecheck` 통과 — 메인 세션이 재실행해 재확인하고 `lib/leg.ts`의 이중 가드 로직을 웹 원본과 직접 대조 검증함 | `apps/mobile/src/lib/{case-notes,leg}.ts`, `apps/mobile/src/screens/{CaseConferenceFormScreen,GuardianshipReportWizardScreen,AdvocacyConsultationFormScreen,LegBoardScreen,SocialWorkerHomeScreen}.tsx`, `apps/mobile/src/navigation/{types,MainNavigator}.tsx`, `docs/09-cto-team-gap-analysis.md`(C-2 완료 상세, Wave C 전체 완료 표시) | 모바일 에뮬레이터 부재로 시각적 확인 미수행 — typecheck + 코드 대조 검증만. Wave A·B·C 전부 종결, 남은 항목은 Wave D(E2E 스팟체크·OAuth·학대신고, 별도 트랙)뿐 |
| 2026-07-18 | `docs/10-livinglab-uiux-workshop.md` 신설 — 사용자 피드백("로그인 후 화면 디자인이 랜딩페이지 대비 세련되지 못함")을 계기로 UIUX팀(PM·UIUX전문가·디자이너·퍼블리셔·프론트엔지니어) + 6개 사용자 그룹(당사자·보호자·특수교사·활동지원사·사회복지사·치료사) 리빙랩 워크숍 시뮬레이션. 코드 전수 대조로 근거 확보: `accent-amber`/`accent-coral`/`shadow-*`/`gradient` 토큰이 랜딩(파일 2개)에 13회, 앱 전체(80여 파일)엔 22회뿐 — 파일당 밀도 격차 확인. 근본 원인 5가지 진단(elevation 토큰 자체가 `docs/03-uiux.md` §4에 부재/accent 컬러 편중 사용/PersonSlider 아바타가 avatarUrl 유무와 무관하게 항상 이모지만 렌더링하는 설계-구현 갭/모션 전무/여백 부족). 핸즈온 중 제기된 "ISP 달성률 실제 progress bar 구현 여부" 확인 요청은 `IspReviewPane.tsx` 재확인 결과 이미 실제 채움 바(`width:${rate}%`)로 구현돼 있음을 확인해 결함 아님으로 종결. **당사자 모드는 접근성 우선 원칙에 따라 이번 Wave 범위에서 제외**(자기표현 완료 애니메이션 스펙 대비 구현 여부만 별도 확인 항목). Wave UI-1(elevation 토큰 정의)→UI-2(PersonSlider·GlobalHeader·Sidebar·RecordContentView 리터치)→UI-3(accent 컬러 CTA 확대)→UI-4(모션 보강, 선택) 개선안 확정 | `docs/10-livinglab-uiux-workshop.md`(신규), `CLAUDE.md`(핵심 문서 표) | 문서 작성만 완료, 실제 구현은 미착수 — 다음 요청 시 Wave UI-1(elevation 토큰 정의)부터 착수 가능. 브라우저 스팟체크는 이번에도 Claude-in-Chrome 페어링 연결 불안정으로 실패해 코드 리뷰 기반 진단으로 대체(라이브 스크린샷 미확보) |
| 2026-07-18 | Wave UI-1(elevation 토큰 정의) 완료 — `docs/03-uiux.md` §4-1 신설. 별도 CSS 변수(`--shadow-*`) 없이 Tailwind v4 기본 `shadow-sm`/`md`/`lg` 유틸리티에 용도(리스트·인풋 포커스/카드/부유 레이어)를 명문화하는 방식 채택 — `AuthCard`가 이미 `shadow-sm`을 쓰고 있어 신규 도입 비용 없음(퍼블리셔 판단). "흰 배경 위 흰 카드는 border 단독이 아니라 shadow-sm 이상을 함께 준다" 원칙 명시, 당사자 모드는 WCAG AAA 원칙에 따라 적용 제외 | `docs/03-uiux.md` §4-1(신규), `docs/10-livinglab-uiux-workshop.md`(Wave UI-1 완료 표시) | 문서화만 완료, 실제 컴포넌트 적용은 Wave UI-2(PersonSlider·GlobalHeader·Sidebar·RecordContentView 리터치)에서 진행 |
| 2026-07-18 | Wave UI-2(핵심 컴포넌트 리터치) 완료 — `PersonSlider`(카드·응급정보 카드·요약 3카드·슬라이더 버튼에 `shadow-md`/`shadow-sm` 적용, 아바타를 이모지 하드코딩에서 실제 `Avatar`/`AvatarImage`/`AvatarFallback`로 교체), `GlobalHeader`(`shadow-sm`+`relative z-10`), `RecordContentView`(웹, 배열 카드 `shadow-sm`+여백 확대) 적용. `Sidebar`는 좌측 accent 바 추가 과정에서 **부수 결함 발견**: `layout.tsx`가 Server Component라 pathname을 몰라 `SidebarItem.active`를 한 번도 채운 적이 없어 사이드바 현재 위치 강조가 원천적으로 동작한 적이 없었음 — `usePathname()`으로 `Sidebar` 자체가 판정하도록 재구성해 근본 해결(`active` prop 제거). PersonSlider 리터치 중 대시보드 "알림" 카드가 "알림 시스템 준비 중" 하드코딩 문구를 그대로 쓰고 있던 것도 발견(오늘 세션 초반 실제 알림함(`/notifications`)을 이미 구현했는데 이 카드만 갱신 누락) — `/notifications` 링크로 교체. 모바일 `RecordContentView`는 RN 플랫폼별 그림자 속성이 에뮬레이터 없이 검증 불가해 이번 Wave에서 보류(명시적 결정, 누락 아님). `pnpm --filter @ongil/web typecheck`·`build` 모두 통과 | `apps/web/src/components/{guardian/PersonSlider,layout/GlobalHeader,layout/Sidebar,records/RecordContentView,guardian/RecordManager}.tsx`, `docs/10-livinglab-uiux-workshop.md`(Wave UI-2 완료 표시) | 다음 착수 대상은 Wave UI-3(accent 컬러 CTA 확대 적용) |
| 2026-07-18 | Wave UI-3(accent 컬러 CTA 확대 적용) 완료 — 역할별 Home 주 CTA 재조사 결과 supporter/teacher/social_worker/therapist/person 5개 역할은 이미 `accent-amber`를 쓰고 있어 **보호자 대시보드만 예외**였음을 확인, `dashboard/page.tsx`의 "+ 당사자 추가"/"당사자 등록하기" 버튼을 다른 역할과 동일한 amber로 통일. `EvalComparisonTable.tsx`(치료사 평가보고서 3열 비교)의 증감 셀을 텍스트 색상 단독 표시에서 배경 칩(pill)으로 승격(증가=도메인 MED accent 톤, 감소=별도 경고색, 변화없음=중립) — 리빙랩 워크숍에서 치료사가 제기한 "표를 스캔할 때 색만으로는 놓치기 쉽다" 피드백을 직접 반영. `pnpm --filter @ongil/web typecheck`·`build` 모두 통과 | `apps/web/src/app/(app)/dashboard/page.tsx`, `apps/web/src/components/therapist/EvalComparisonTable.tsx`, `docs/10-livinglab-uiux-workshop.md`(Wave UI-3 완료 표시) | 다음 착수 대상은 Wave UI-4(모션 보강, 선택) |
| 2026-07-18 | Wave UI-4(모션 보강) 완료로 **리빙랩 UIUX 워크숍 Wave UI-1~4 전체 종결**. Sidebar 접힘/DropdownMenu 열림/닫힘은 이미 Tailwind v4 기본 150ms duration을 쓰고 있어 조정 불필요했음을 확인 — 실제 갭은 `prefers-reduced-motion` 전역 처리가 전무했다는 점이라, `globals.css`에 미디어 쿼리 1곳으로 모든 animation/transition 유틸리티에 일괄 적용(컴포넌트별 개별 처리 대신 전역화, 신규 컴포넌트도 자동 커버). 자기표현(P-02) 완료 애니메이션은 워크숍 1차 관찰에서 "미구현 의심"으로 남겼으나 `SaveOverlay.tsx` 직접 확인 결과 confetti·bounce·2초 자동 리다이렉트까지 이미 완전 구현돼 있었음을 확인·정정(오탐, 결함 아님). `pnpm --filter @ongil/web typecheck`·`build` 모두 통과 | `apps/web/src/app/globals.css`, `docs/10-livinglab-uiux-workshop.md`(Wave UI-4 완료 표시, §3 오탐 정정, 전체 종결 표시) | Wave UI-1(elevation 토큰)→UI-2(핵심 컴포넌트 리터치)→UI-3(accent CTA)→UI-4(모션) 전 과정에서 시각 디자인 리뷰가 실제 기능 결함(사이드바 활성 강조 미동작, 대시보드 알림 카드 stale 문구, 보호자 대시보드 CTA 색상 누락)을 함께 드러낸 것이 이번 워크숍의 부수 성과 |
| 2026-07-18 | `docs/11-livinglab-mega-workshop.md` 신설 — `docs/10`(웹 UIUX 단일 초점) 후속으로 범위를 확대해 PM·PL + CTO팀 10인(`docs/09`와 동일 편성) + 사용자 12명(6역할×2명, 신입/숙련·단일/다자녀 등 동일 역할 내 다른 사용 패턴까지 반영) 리빙랩 시뮬레이션. 핵심 발견: ① **모바일이 Wave UI-1~4에서 완전히 제외돼 웹·모바일 시각 격차가 오히려 벌어짐**(모바일 전체 코드베이스에 `shadowColor`/`elevation` 사용 0건 실측 확인) ② 모바일에 알림함이 아예 없음(웹은 최근 신설, 화면 수는 51:51로 동일해도 최신 기능은 모바일이 못 따라감) ③ DB전문가가 actions.ts 반복문 26곳 스팟체크 결과 N+1 아님(오탐)으로 결론 ④ QA가 Claude-in-Chrome 페어링 불안정을 3세션 연속 문제로 정식 이슈화. 사용자 B(청소년 전환기 당사자, 보호자 대리 시연)가 제기한 "ITP를 당사자가 볼 방법이 없어 보인다"는 관찰을 CTO팀이 즉시 `records_select` RLS 원문 조회로 검증 — person 셀프 분기는 domain/record_type 무관하게 본인 기록 전체를 이미 커버하고 있어 **RLS 결함이 아니라 UX 발견성 문제로 재정의**(추측 아닌 라이브 정책 조회로 확인). Wave M-1(모바일 알림함 이식, 최우선)→M-2(당사자 ITP 발견성 개선)→M-3(소규모 UI 보완 묶음)→M-4(모바일 elevation/모션, 실기기 확보 시까지 조건부 보류)→M-5(OAuth 구현 우선순위 상향, 기존 Wave D-2 승격) 로드맵 확정 | `docs/11-livinglab-mega-workshop.md`(신규), `CLAUDE.md`(핵심 문서 표) | 문서 작성만 완료, 실제 구현은 미착수 — 다음 요청 시 Wave M-1(모바일 알림함 이식)부터 착수 가능 |
| 2026-07-18 | Wave M-1(모바일 알림함 이식) 완료 — 웹 `notifications/actions.ts`를 모바일 `lib/notifications.ts`로 1:1 포팅(기존 이식 컨벤션·`NOTIFICATION_TYPE_LABEL` 라벨 사전 포함), `NotificationListScreen`(목록·읽음 처리·모두 읽음, `HandoverListScreen` 스타일 재사용, route params 없어 7개 역할 스택 어디서든 동일 컴포넌트 등록 가능) 신설. 공용 `NotificationBell`(아이콘+배지, onPress만 주입받는 스택 비의존 설계)을 supporter/guardian/teacher/social_worker/therapist Home에 배치, person(당사자) 모드는 접근성 원칙에 따라 예외적으로 라벨형 보조 CTA로 별도 구현. `navigation/types.ts`의 7개 StackParamList와 `MainNavigator.tsx`의 7개 Navigator 전부에 `Notifications` 라우트 등록(Generic 스택 포함). `pnpm --filter @ongil/mobile typecheck` 통과 | `apps/mobile/src/lib/notifications.ts`(신규), `apps/mobile/src/screens/NotificationListScreen.tsx`(신규), `apps/mobile/src/components/NotificationBell.tsx`(신규), `apps/mobile/src/screens/{PersonHome,SupporterHome,GuardianDashboard,TeacherHome,SocialWorkerHome,TherapistHome}Screen.tsx`, `apps/mobile/src/navigation/{types,MainNavigator}.tsx`, `docs/11-livinglab-mega-workshop.md`(Wave M-1 완료 표시) | 모바일 에뮬레이터 부재로 시각적 확인은 미수행(typecheck만). 다음 착수 대상은 Wave M-2(당사자 ITP 발견성 개선) |
| 2026-07-18 | Wave M-2(당사자 ITP 발견성 개선) 완료 — P-10(웹 `PersonRecordsView.tsx`·모바일 `MyRecordsScreen.tsx`) 목록 최상단에 조건부 안내 배너 추가(`lifeStage==="youth_transition"` && `EDU-005` 보유 시 "🎓 학교에서 준비한 개별화전환계획(ITP)이 있어요"). RLS·백엔드는 이미 정상(Wave M-2 계획 수립 시 §6에서 검증 완료)이라 순수 UX 발견성 개선. **웹·모바일을 처음부터 동시 구현** — 이번 워크숍이 지적한 "웹만 개선하고 모바일을 나중에 빼먹는 패턴"을 이 Wave부터 반복하지 않기 위한 의도적 결정. 웹은 `bg-domain-tra-bg`, 모바일은 `DOMAIN_COLORS.TRA`(SSOT)로 동일 톤 재현. `pnpm typecheck`(web·mobile)·`pnpm --filter @ongil/web build` 모두 통과 | `apps/web/src/components/person/PersonRecordsView.tsx`, `apps/mobile/src/screens/MyRecordsScreen.tsx`, `docs/11-livinglab-mega-workshop.md`(Wave M-2 완료 표시) | 다음 착수 대상은 Wave M-3(소규모 UI 보완 묶음) |
| 2026-07-18 | Wave M-3(소규모 UI 보완 묶음) 완료 — ① `PersonSlider` 3명+ 그리드 뷰: 카드를 `PersonCard`로 추출해 슬라이더/그리드 재사용, 3명 이상일 때만 "⊞ 그리드로 보기" 토글 노출 ② `TeacherHome`에 기존 "전환계획 수립 대상"(13세+ 전체)과 별개로 `isItpActiveStage`로 좁힌 "🎓 ITP 대상" 배지 추가(웹·모바일 동시) ③ `SocialWorkerHome` LEG/사례회의록 진입점을 새 `CaseManagementMenu`(base-ui DropdownMenu 재사용, Server Component인 SocialWorkerHome에서 분리한 별도 `"use client"` 파일) 드롭다운 하나로 통합. 모바일은 두 버튼이 이미 같은 행에 나란히 배치돼 있어 웹만큼 문제가 심하지 않다고 판단해 변경하지 않음(RN에 동급 드롭다운 프리미티브 부재, 억지 구현은 배보다 배꼽). `pnpm typecheck`(web·mobile)·`pnpm --filter @ongil/web build` 모두 통과 | `apps/web/src/components/guardian/PersonSlider.tsx`, `apps/web/src/components/teacher/TeacherHome.tsx`, `apps/mobile/src/screens/TeacherHomeScreen.tsx`, `apps/web/src/components/social-worker/{SocialWorkerHome,CaseManagementMenu}.tsx`(후자 신규), `docs/11-livinglab-mega-workshop.md`(Wave M-3 완료 표시) | 다음 착수 대상은 Wave M-4(모바일 elevation/모션, 실기기 확보 시까지 조건부 보류) 또는 Wave M-5(OAuth 구현 우선순위 상향) |
| 2026-07-18 | Wave M-4는 실기기/에뮬레이터 미확보로 계속 보류 재확인(사용자에게 구두 고지 후 스킵). **Wave M-5(F-AUTH-02 카카오/네이버 OAuth) 코드 구현 완료** — DB: `AuthProvider` enum + `users.auth_provider`/`oauth_subject`/`email_verified` 3컬럼 + `(auth_provider, oauth_subject)` UNIQUE/partial index 마이그레이션, `handle_new_user()` 트리거가 OAuth 메타데이터도 읽도록 갱신(role 없는 신규 OAuth 유저는 기존 "안전장치" 설계대로 `public.users` 행 생성을 건너뛰고 온보딩에서 완성). 컬럼 GRANT 상속 여부를 재검토했으나 `users`에 INSERT RLS 정책 자체가 없어 신규 취약점 아님을 라이브 쿼리로 확인. 백엔드: 카카오·네이버 둘 다 Supabase 네이티브 provider 대신 동일한 커스텀 OAuth2 Authorization Code 브리지로 구현(카카오도 OIDC 대신 이 패턴 채택 — GoTrue 버전 의존성 없이 완전 자체검증 가능하게 하려는 의도적 결정), `/api/auth/{kakao,naver}`(인가요청)·`/api/auth/{kakao,naver}/callback`(토큰교환·프로필조회) 4라우트 + 공용 로직 `oauth-bridge.ts`(계정매칭→세션수립→이메일계정 충돌 시 계정탈취 방지 안내→신규 시 role 없이 생성 후 온보딩). 프론트: `SocialAuthButtons`(`/login`·`/signup`)·`/signup/oauth-complete`+`OAuthCompleteForm`+서버액션 `completeOAuthSignup`(멱등 가드 포함) 신설. **라이브 테스트로 실버그 발견·수정**: `origin`을 `Origin` HTTP 헤더에서 구하던 최초 구현이 일반 GET 네비게이션엔 그 헤더가 오지 않아 4라우트 전부 500(`ERR_INVALID_URL`) 유발하는 것을 실제 재현·확인, `req.nextUrl.origin`으로 교체해 전부 수정 — 수정 후 typecheck·build(48라우트) 통과, dev 서버 기동해 `/api/auth/{kakao,naver}` curl로 자격증명 미설정 시 307 정상 리디렉션(500 아님) 및 `/login?error=...` 배너 렌더링까지 실측 확인 | `supabase/prisma/migrations/20260719000000_p3_oauth_provider_fields/`, `supabase/prisma/migrations/20260719010000_p3_oauth_signup_trigger/`(신규 2건), `supabase/prisma/schema.prisma`, `apps/web/src/lib/oauth-bridge.ts`(신규), `apps/web/src/app/api/auth/{kakao,naver}/{route.ts,callback/route.ts}`(신규 4건), `apps/web/src/app/(auth)/actions.ts`, `apps/web/src/app/(auth)/signup/oauth-complete/page.tsx`(신규), `apps/web/src/components/auth/{OAuthCompleteForm,SocialAuthButtons}.tsx`(신규), `apps/web/src/app/(auth)/{login,signup}/page.tsx`, `apps/web/.env.local`(주석 안내만), `docs/11-livinglab-mega-workshop.md`(Wave M-4/M-5 완료 표시) | **한계**: 카카오/네이버 개발자센터 앱 등록은 플랫폼 소유자만 가능한 외부 작업이라 이 세션에서 실제 토큰교환·프로필조회·계정생성까지 이어지는 end-to-end 로그인은 라이브 검증되지 않음 — 실 자격증명 확보 후 별도 라운드 재검증 필요. 이번 라운드로 `docs/11-livinglab-mega-workshop.md`의 Wave M-1~M-5 로드맵 전체가 종결(M-4만 조건부 보류 상태 유지) |
| 2026-07-19 | UIUX팀에 `/prototypes/` 원본(web/app × 6역할 + common, 14개 HTML) ↔ 실구현(`apps/web`, `apps/mobile`) 정합성 갭 분석 위임 — 6개 역할 병렬 서브에이전트로 착수했으나 계정 월간 사용한도 도달로 절반(당사자·특수교사·사회복지사·치료사)만 서브에이전트가 완료하고 나머지(보호자·활동지원사)는 메인 세션이 직접 이어받아 완료. **P1 없음**(성숙한 코드베이스 확인), P2 갭은 5가지 근본 패턴으로 수렴: ①모바일 하단 탭바·전용 설정화면 부재(당사자 제외 5역할 반복) ②타임라인 응급정보 고정카드가 보호자 전용으로만 배선(특수교사·사회복지사 안전정보 사각) ③모바일 온보딩·스플래시 전무 ④크로스플랫폼 색상·스타일 역전 2건(당사자 CTA·치료사 목표마커) ⑤회기/방문 스케줄 섹션 부재(스케줄링 백엔드 없음, 별도 트랙). 결과는 `_workspace/11_prototype_gap_analysis.md`. 사용자가 Wave P-1~P-4 전체 착수를 확정해 즉시 구현 — **P-1** 모바일 `SplashScreen`(브랜드 모션)·`OnboardingScreen`(3슬라이드, AsyncStorage 최초 1회) 신설, `App.tsx`가 세션 유무·온보딩 확인 여부로 분기(재방문·로그인 사용자는 지연 없음) **P-2** 타임라인 응급정보 PinnedCard를 보호자 전용에서 특수교사·사회복지사·치료사까지 확장 — 웹 `timeline/page.tsx`는 `persons.emergency_info` 직접 조회 추가, 모바일은 `getPersonEmergencyInfo`(`lib/iep.ts` 신설, `lib/isp.ts`·`lib/therapy.ts`가 재수출)를 3개 타임라인 화면에 배선 **P-3** 당사자 모바일 홈 CTA를 웹과 동일한 accent-amber로 통일, 치료사 웹에 "핵심" 목표 마커·저달성 60% 미만 "⚠️ 집중 필요" 경고 스타일을 모바일 기준으로 역이식 **P-4**(최대 범위, 사용자가 "하단 탭바까지 전체 구현" 명시 선택) `@react-navigation/bottom-tabs` 신규 설치 후 특수교사·사회복지사·치료사·보호자·활동지원사 5역할 전부에 실제 Bottom Tab Navigator 도입(`navigation/RoleTabNavigators.tsx` 신설) — 기존 Home 화면은 타입 캐스트 어댑터로 그대로 재사용(런타임은 상위 Stack까지 정상 버블링), 대상자 선택이 필요한 2차 탭(IEP/ISP/전환/치료계획/회기일지/평가/보호자 타임라인·기록·권한)은 공용 `PersonPickerScreen`(대상자 목록→목적 화면) 신설로 통일, 활동지원사 "일지" 탭은 `JournalListScreen`(전체 일지 목록, 신규) 도입, 5역할 공용 `RoleSettingsScreen`(프로필·알림·개인정보동의·도움말·로그아웃) 신설해 기존 Home 화면들의 인라인 "로그아웃" 텍스트 버튼을 제거하고 설정 탭으로 통합, `PrivacySettings`를 특수교사·사회복지사·치료사·활동지원사 스택에도 신규 등록(기존엔 당사자·보호자만 보유) | `_workspace/11_prototype_gap_analysis.md`(신규), `apps/mobile/src/screens/{SplashScreen,OnboardingScreen,PersonPickerScreen,RoleSettingsScreen,JournalListScreen}.tsx`(전부 신규), `apps/mobile/App.tsx`, `apps/mobile/src/navigation/{types,MainNavigator,RoleTabNavigators}.tsx`(마지막 신규), `apps/mobile/src/lib/{iep,isp,therapy}.ts`, `apps/mobile/src/screens/{Teacher,SocialWorker,Therapist,Supporter}HomeScreen.tsx`·`GuardianDashboardScreen.tsx`(로그아웃 버튼 제거)·`{Edu,Wel,Med}TimelineScreen.tsx`(응급정보 배선), `apps/web/src/app/(app)/timeline/page.tsx`, `apps/web/src/components/timeline/{TimelineView,TimelineStream}.tsx`(주석 정정), `apps/web/src/components/therapist/{TherapyPlanWizard,TherapyPlanDetail}.tsx`, `apps/mobile/package.json`(`@react-navigation/bottom-tabs` 추가) | `pnpm typecheck`(web·mobile)·`pnpm --filter @ongil/web build` 전부 통과. **한계**: 이번 세션도 모바일 실기기·에뮬레이터가 없어 탭 전환·픽커 화면·설정 화면의 실제 렌더링/터치 동작은 라이브 검증하지 못했다(타입체크 통과로만 확인) — 특히 P-4는 이 프로젝트에서 가장 큰 미검증 모바일 네비게이션 변경이므로, 실기기 확보 시 최우선 스팟체크 대상으로 남긴다. Wave P-5(치료사 회기·활동지원사 방문 스케줄, 백엔드 선행 필요)는 이번 라운드 범위 밖으로 명시적으로 보류 |
| 2026-07-19 | `docs/12-content-fidelity-workshop.md` 신설 — 앞선 라운드(`_workspace/11_prototype_gap_analysis.md` + Wave 1/2/P3)가 "화면·메뉴 존재 여부" 레벨이었다면, 이번엔 사용자가 보호자 대시보드(G-01)를 구체 예시로 지목하며 요구한 "카드 내부 콘텐츠 구조·타이포그래피" 레벨 정합성을 다룬다. G-01을 완전 해부(Exhibit A)한 결과, 당사자 카드 상단 통계(이번주 기록/권한부여/D-day)가 통째로 없고, "최근 기록"·"권한 현황"·"확인 대기 기록" 3개 카드가 전부 "목록"이 아니라 "숫자 하나"로 축약돼 있었으며(`getPersonSummaryCards`가 count만 반환, `PendingConfirmCard`도 마찬가지), "최근 기록"은 기록명 대신 `record_type` 코드값을 그대로 노출하고 작성자가 아예 빠져 있었다. 프로토타입 CSS 원문에서 폰트크기·패딩·행 구분선 등 타이포그래피 체크리스트도 추출했다. "알림" 카드는 프로토타입 원문 CTA(`권한 부여하기`)가 알림과 무관해 원본 자체의 실수로 판단, 사용자가 직접 지정한 "알림함 보기"를 채택하기로 명시적으로 결정(유일한 의도적 원문 이탈, 근거를 문서에 기록). Wave A(백엔드 목록화 5건, D-day 산출 기준은 도메인별 필드가 흩어져 있어 결정 대기)→B(프론트 재구성)→C(T-01/W-01/TH-01/S-01 등 같은 패턴 화면 확장 적용) 개선계획 수립 | `docs/12-content-fidelity-workshop.md`(신규), `CLAUDE.md`(핵심 문서 표) | 문서 작성만 완료, 구현은 미착수 — 사용자가 Wave A부터 진행을 확정하면 `getPersonSummaryCards` 확장부터 착수 |
| 2026-07-20 | `docs/12-content-fidelity-workshop.md` Wave A~C 구현(보호자 대시보드 카드 실제 목록화) 이후, 세션이 웹 UI 개선을 계속 확장 — 기록 상세보기 표/그래프 시각화, 계정 전환 콤보박스, 프로필 수정 메뉴, 보호자의 당사자 정보 수정·목록 제외, 헤더/사이드바 재구조화(알림은 우상단, 사용자 메뉴는 좌측 하단)를 순차 반영한 뒤, 활동지원 일지 작성 화면을 웹 디자인 언어에 맞게 재설계한 것을 계기로 **"모든 기록 작성화면을 와이드 레이아웃으로 재구성해달라"**는 요청을 받아 웹 16개 기록작성 화면 전체(`RecordTypePicker`·`RecordForm`·`PersonRegisterWizard`·`PermissionGrantWizard`·`IepWizard`·`ItpWizard`·`TherapyPlanWizard`·`IspWizard`·`TransitionPlanWizard`·`GuardianshipReportWizard`·`BipForm`·`ObservationForm`·`SessionNoteForm`·`EvalReportForm`·`CaseConferenceForm`·`AdvocacyConsultationForm`)를 `mx-auto max-w-6xl` + `lg:grid-cols-[1fr_340px]` sticky 사이드바 패턴(확인배너·요약·미리보기 등 폼별 맥락 콘텐츠)으로 통일. 이어서 **"모바일도 같은 방식으로 적용해줘"** 요청에 대해 RN은 폰 폭 특성상 2단 그리드가 불가능함을 이유로 AskUserQuestion으로 범위 확인("전체 15개 화면(권장)" 선택) 후, 공용 `Card.tsx`(rounded-lg·shadow) 신설 + 웹 16개와 대응하는 모바일 15개 화면(BIP·관찰기록은 메인 세션이 직접, 나머지 13개는 mobile-dev 서브에이전트에 위임) 전체에 섹션 단위 카드 분리 + 도메인 색상(`DOMAIN_COLORS`) 로컬 테마링을 적용 | `apps/web/src/components/{guardian,teacher,therapist,social-worker}/*Wizard*.tsx`·`*Form*.tsx`(16개), `apps/web/src/components/records/RecordContentView.tsx`, `apps/web/src/components/layout/{AccountSwitcher,Sidebar}.tsx`(신규/수정), `apps/web/src/app/(app)/settings/profile/**`(신규), `apps/web/src/app/(app)/dashboard/actions.ts`, `apps/mobile/src/components/Card.tsx`(신규), `apps/mobile/src/screens/{Bip,Observation,Itp,Iep,TherapyPlan,SessionNoteForm,EvalReport,Isp,CaseConferenceForm,TransitionPlan,GuardianshipReportWizard,AdvocacyConsultationForm,PermissionGrant,PersonRegister,RecordForm}*.tsx`(15개) | 브라우저 자동화가 이 세션에서도 `localhost` 프레임을 계속 못 띄워(환경 문제, 코드 문제 아님) 웹 검증은 실 JWT+curl API 검증으로 대체했고, 모바일은 에뮬레이터 부재로 `pnpm typecheck` 통과로만 검증(둘 다 시각적 렌더링 미확인 — 실기기/브라우저 페어링 확보 시 최우선 스팟체크 대상). `TransitionPlanWizard.tsx` 재구성 중 중복 `</div>`로 인한 TS1005/1109 구문 오류를 typecheck에서 즉시 발견·수정. RLS 쪽에서도 이 라운드 중 두 갭을 추가 발견·즉시 수정: ① `users_select_own`이 자기 자신만 허용해 `author:users!author_id(full_name)` 임베디드 조인이 타인에겐 항상 null이던 것을 `persons_select`와 동형의 "공유 이해관계자" 패턴으로 `users_select_related` 신설해 해소 ② `persons_update`/`guardians` DELETE 정책이 아예 없던 것을 REVOKE→GRANT 컬럼 제한 원칙(과거 `users` 컬럼 GRANT 사고 교훈)으로 신설, 라이브 트랜잭션 롤백 테스트로 주보호자 위조 차단·공동보호자 자진탈퇴 허용을 확인 |
| 2026-07-20 | 특수교사 로그인 시 사이드바 "IEP 점검"·"홈"이 둘 다 `/home`으로 연결되도록 설계돼 있는데(전용 목록 화면이 없어 의도된 구조) `Sidebar.tsx`가 href만으로 li key를 생성해 React 중복 키 콘솔 에러 발생 — key를 `href::label` 조합으로 수정. 이어서 보호자 헤더에 프로토타입 `.person-select` 콤보박스를 이식 — `CurrentPersonProvider`(클라이언트 Context)로 보호자 레이아웃 전체를 감싸 헤더 콤보박스·사이드바(`GuardianSidebar`)·대시보드 `PersonSlider`가 같은 "현재 당사자" 상태를 공유하게 했다(과거엔 사이드바가 항상 첫 번째 당사자로 고정 연결됐던 알려진 갭 해소). 초기값은 쿠키(`ongil_current_person`)로 재방문 시 복원, `persons_select` RLS로 소유권을 암묵 검증. 이어서 UIUX팀(전문가4+페르소나4)에게 "보호자=당사자 중심/전문직군=업무 중심 선택"이라는 사용자 가설의 교차 검토를 위임 — 3개 병렬 조사(전문직군 선택 반복 횟수, 보호자 웹·모바일 격차, 당사자모드·공통UI)를 근거로 토론한 결과 가설의 방향성은 맞으나 현재 구현은 "업무 중심"이 아니라 "선택 상태 자체가 없는" 상태에 가깝다는 결론(같은 화면 체인에서도 불필요한 재선택 발생, 모바일 탭 전환마다 완전 리셋) — "매번 재선택"은 사회복지사·활동지원사 페르소나가 안전장치로 지지해 유지하되, 최근 선택 힌트로 보완하는 하이브리드로 결론, Wave Q-1~Q-6 로드맵 수립(구현 미착수) | `apps/web/src/components/layout/Sidebar.tsx`, `apps/web/src/app/(app)/dashboard/actions.ts`, `apps/web/src/app/(app)/layout.tsx`, `apps/web/src/components/guardian/{CurrentPersonProvider,GuardianSidebar,PersonHeaderSelect}.tsx`(신규), `apps/web/src/components/guardian/PersonSlider.tsx`, `apps/web/src/components/layout/GlobalHeader.tsx`, `apps/web/src/lib/current-person.ts`(신규), `docs/13-cross-role-person-selection-workshop.md`(신규) | `pnpm typecheck`·`pnpm --filter @ongil/web build` 통과. 당사자 선택 콤보박스는 guardian1 계정으로 RLS 라이브 조회(persons 2건)까지 확인했으나 브라우저 자동화가 이 세션에서도 localhost를 못 띄워 실제 클릭 동작은 미검증. Wave Q는 문서화만 완료, 다음 요청 시 Q-1(웹 관찰기록·타임라인 personId 누락 수정)부터 착수 가능 |
| 2026-07-20 | `docs/13` Wave Q-1 구현 — 교사/사회복지사/치료사 Home 화면 담당 학생·당사자 카드에 "관찰기록"(교사)·"회기 일지"(치료사)·"타임라인"(공통) 퀵링크를 personId와 함께 추가(카드 전체 `<Link>`를 `<div>`+내부 `<Link>`로 재구성해 중첩 `<a>` 방지). 이미 특정 당사자가 정해진 상세 화면(`IepReviewPane`·`IspReviewPane`·`TherapyPlanDetail`)에도 "타임라인 보기" 버튼 추가(`IepReviewPane`은 관찰기록 작성 버튼도 함께). 재선택 자체를 없애지 않고 "이미 특정 당사자를 보고 있는 화면→관련 화면" 전이에서만 재질문을 생략하는 워크숍 결론(§3)을 그대로 적용 — Home 상단 공용 액션 버튼(특정 학생 맥락 없음)은 그대로 둠 | `apps/web/src/components/{teacher/{TeacherHome,IepReviewPane},social-worker/{SocialWorkerHome,IspReviewPane},therapist/{TherapistHome,TherapyPlanDetail}}.tsx`, `docs/13-cross-role-person-selection-workshop.md`(§5 신설, Q-1 완료 표시) | `pnpm typecheck`·`build` 통과. 다음 착수 대상은 Q-4(보호자 모바일 그리드뷰·StageBadge 패리티) |
| 2026-07-20 | `docs/13` Wave Q-4 구현 — 모바일 보호자 대시보드(`GuardianDashboardScreen.tsx`)에 웹 `PersonSlider.tsx`와 동일 규칙(3명 초과 시에만 노출)의 그리드뷰 토글 추가(기존엔 인원수 무관 항상 슬라이더만). 카드 렌더링을 `PersonCard`로 추출해 슬라이더·그리드 양쪽 재사용, "성년" 텍스트 태그를 웹과 동일한 `StageBadge`(5단계 생애주기 배지)로 교체해 정보 밀도 격차 해소 | `apps/mobile/src/screens/GuardianDashboardScreen.tsx`, `docs/13-cross-role-person-selection-workshop.md`(§6 신설, Q-4 완료 표시) | `pnpm --filter @ongil/mobile typecheck` 통과. 에뮬레이터 부재로 그리드 레이아웃 실제 렌더링은 미검증. 다음 착수 대상은 Q-2/Q-3(전문직군 최근 선택 힌트 + 제출 직전 재확인 배너) |
| 2026-07-20 | `docs/13` Wave Q-2/Q-3 구현(웹만) — 전문직군 대상자 select가 있는 폼 11개+목록형 허브 2개 전부에 적용. Q-2: 공용 훅 `usePersonSelection`(`hooks/useRecentPerson.ts`) 신설, 13개 파일의 동일한 `useState(initialPersonId ?? list[0])` 초기화 블록을 교체 — `initialPersonId` 없을 때만 마운트 후 localStorage(`ongil_recent_person`)의 "최근 선택"으로 기본값 보정(초기 렌더는 항상 서버와 동일해 하이드레이션 불일치 회피). Q-3: 공용 컴포넌트 `TargetPersonBanner`(`components/records/`) 신설, 작성 폼 11개(목록형 2개 제외)의 저장 버튼 바로 위에 "🎯 제출 대상: {이름}" 강조 표시 — select 자체는 유지해 재선택 경로를 없애지 않음(§2 토론 결론) | `apps/web/src/hooks/useRecentPerson.ts`(신규), `apps/web/src/components/records/TargetPersonBanner.tsx`(신규), `apps/web/src/components/{teacher/{IepWizard,ItpWizard,ObservationForm,BipForm},therapist/{TherapyPlanWizard,EvalReportForm},social-worker/{IspWizard,TransitionPlanWizard,GuardianshipReportWizard,AdvocacyConsultationForm,CaseConferenceForm,CaseNotesBoard,LegRecordsBoard}}.tsx`(13개), `docs/13-cross-role-person-selection-workshop.md`(§7 신설, Q-2/Q-3 완료 표시) | `pnpm typecheck`·`build` 통과. 모바일(PersonPickerScreen)은 이번 라운드 미포함 — 후속 라운드로 이월. 다음 착수 대상은 Q-6(사이드바/탭바 항목 수 정리) 또는 Q-5(당사자 알림 경로) |
| 2026-07-20 | `docs/13` Wave Q-6 구현 — 착수 전 재조사 결과 지목된 3건 중 2건(교사 "IEP 점검", 활동지원사 "일지 작성/목록")은 이미 웹과 동등하거나 모바일다운 통합이라 결함 아님으로 결론, 실제 갭 1건만 수정: 사회복지사 모바일에 "인수인계" 진입점이 전혀 없었음(웹 사이드바엔 있음, `SocialWorkerStackParamList`에 `HandoverList`/`HandoverCompose` 라우트 자체가 미등록). `SupporterStackParamList`와 동형으로 라우트 추가 + 공용 `HandoverListScreen`/`HandoverComposeScreen`을 다른 Home 화면 재사용 패턴과 동일한 props 캐스트로 `SocialWorkerStack`에 등록, `SocialWorkerHomeScreen`에 "🔁 인수인계" 진입 버튼 추가 | `apps/mobile/src/navigation/{types,MainNavigator}.tsx`, `apps/mobile/src/screens/SocialWorkerHomeScreen.tsx`, `docs/13-cross-role-person-selection-workshop.md`(§8 신설, Q-6 완료 표시) | `pnpm --filter @ongil/mobile typecheck` 통과. Wave Q 로드맵 중 Q-5(당사자 모드 알림 경로, 접근성 재검토 선행)만 남음 |
| 2026-07-20 | 사용자 피드백 — 보호자가 기록작성 화면(RecordTypePicker `/persons/{id}/records/new` → 구조화 서식 `/records/{type}/new?personId=`)에 있는 상태에서 헤더 콤보박스로 당사자를 바꿔도 화면은 이전 당사자에 그대로 머물러 있던 문제 수정. `CurrentPersonProvider.setPersonId`가 현재 pathname을 검사해 ① `/persons/{id}/...`이고 하위 경로가 "안전 목록"(timeline/records/records/new/records/express/permissions/permissions·grant/access-logs)이면 새 당사자 URL로 `router.replace` ② URL에 `personId` 쿼리가 있으면(구조화 서식들) 쿼리 값만 갱신해 같은 화면을 유지한 채 대상만 전환. `/persons/{id}/records/{recordId}(/edit)`처럼 특정 레코드를 가리키는 경로는 당사자가 바뀌면 의미가 달라지므로 안전 목록에서 의도적으로 제외했다. 쿼리 갱신만으로는 페이지 컴포넌트가 리마운트되지 않아 `usePersonSelection`(Q-2에서 신설한 훅)의 로컬 state가 새 `initialPersonId` prop을 못 따라가는 문제가 있어, `initialPersonId` 변경을 감시하는 effect를 추가로 넣었다(전문직군 폼에도 같은 훅을 쓰지만 그쪽은 이 prop이 마운트 후 안 바뀌어 영향 없음) | `apps/web/src/components/guardian/CurrentPersonProvider.tsx`, `apps/web/src/hooks/useRecentPerson.ts` | `pnpm typecheck`·`build` 통과. `useSearchParams()` 훅 대신 이벤트 핸들러 안에서 `window.location`을 직접 읽는 방식을 택했다(이 코드베이스의 기존 관례상 `useSearchParams()`는 Suspense 경계가 필요한데, 레이아웃 전체를 감싸는 Provider에 Suspense를 두면 원치 않는 로딩 상태가 생긴다). 브라우저 자동화가 이 세션에서도 localhost를 못 띄워 실제 클릭 시나리오는 라이브 검증하지 못함 |
| 2026-07-20 | 활동지원사·특수교사·사회복지사·치료사 좌측 사이드바에서 "설정" 항목 제거(헤더 계정 드롭다운에 이미 있는 진입점이라 중복, 기본값 role 사이드바는 범위 밖이라 유지). 이어서 PO 제안("전문직군 홈에 오늘 할 일이 아니라 이번 주 처리할 일을 보여주면 어떨까")을 검토 — 실제로는 "오늘" 기준 방문·회기 일정 데이터가 DB에 전혀 없어 "이번 주"가 더 현실적이라는 데 합의 후, `docs/14-weekly-tasks-workshop.md` 신설. `packages/validation/src/records.ts` 전수조사 결과 EDU-003(BIP)·EDU-005(ITP)·WEL-004(ISP)·WEL-005(서비스이용현황)·TRA-001(전환계획)·LEG-001(후견감독보고서)엔 마감일 필드가 있으나 EDU-001(IEP)·MED-005(치료계획서)·MED-007(평가보고서)·활동지원 일지엔 전혀 없음을 확인 — 특수교사·사회복지사는 "마감일 기반", 치료사·활동지원사는 "백로그 기반"(치료사=계획서 미작성 대상자, 활동지원사=기존 SupporterHome의 "임시저장 N건" 통계를 목록으로 승격)으로 카드 성격을 이원화하는 결론. 사회복지사는 WEL-005·LEG-001 마감일 필드가 지금까지 홈 어디서도 조회되지 않고 있었다는 부수 발견도 있었음. Wave W-1~W-4 로드맵 수립(구현 미착수) | `docs/14-weekly-tasks-workshop.md`(신규), `CLAUDE.md`(핵심 문서 표) | 문서 작성만 완료 — 사용자가 진행 확정하면 W-1(백엔드 조회 함수)부터 착수 |
| 2026-07-20 | `docs/14` Wave W-1/W-2/W-3 구현(웹만) — 특수교사·사회복지사 홈에 마감일 기반 "📅 이번 주 처리할 일" 카드, 치료사·활동지원사 홈에 백로그 기반 "📋 처리 대기 중" 카드 신설. 공용 유틸 `lib/weekly-tasks.ts`(D-7 이내+기한초과="이번 주")·컴포넌트 `WeeklyTaskCard`/`BacklogTaskCard` 신설. 백엔드 보강: `TransitionClient.latestPlan`에 `nextReviewDate` 추출 추가(기존 미조회), `getServiceUsageNextReviewDates()` 신설(WEL-005 record 레벨 `next_review_date`가 지금까지 `services[]` 평탄화 조회에 가려 전혀 안 쓰이고 있었음) — `getLegClients`/`getBipClients`/`getItpClients`는 이미 필드를 갖고 있어 재사용만 함. `SocialWorkerHome`의 기존 `TodayTasks`(ISP만)는 4종 통합 카드로 완전 대체, `SupporterHome`은 기존 "임시저장 N건" 통계를 유지한 채 목록형 카드를 추가 배치 | `apps/web/src/lib/weekly-tasks.ts`(신규), `apps/web/src/components/records/{WeeklyTaskCard,BacklogTaskCard}.tsx`(신규), `apps/web/src/app/(app)/records/{transition,isp}/actions.ts`, `apps/web/src/components/{teacher/TeacherHome,social-worker/SocialWorkerHome,therapist/TherapistHome,supporter/SupporterHome}.tsx`, `docs/14-weekly-tasks-workshop.md`(§5 신설, W-1~W-3 완료 표시) | `pnpm typecheck`·`build` 통과. social1 계정으로 WEL-005/TRA-001 `next_review_date` 필드가 seed 데이터에 실제로 채워져 있음을 라이브 REST 조회로 확인(LEG-001은 이 계정에 seed 데이터 없음 — 코드 문제 아님). seed 날짜가 전부 2026-10-15라 현재 "이번 주" 창에는 안 걸림. W-4(모바일)는 이번 라운드 범위 밖 |
| 2026-07-20 | `docs/14` Wave W-4 구현(모바일) — 웹 4개 요소(`WeeklyTaskCard`/`BacklogTaskCard`/`lib/weekly-tasks.ts`/백엔드 보강)를 1:1 이식, `docs/14` Wave W-1~W-4 전체 종결. 차이는 `href` 대신 `onPress`(RN 네비게이션은 route name+params라 각 홈 화면이 직접 콜백 구성)뿐. `lib/transition.ts`(`TransitionClient.latestPlan.nextReviewDate` 추출 누락이 모바일에도 동일하게 있었음)·`lib/isp.ts`(`getServiceUsageNextReviewDates()` 신설)도 웹과 동형으로 보강. `TeacherHomeScreen`/`SocialWorkerHomeScreen`엔 `WeeklyTaskCard`(사회복지사는 기존 `TodayTasks` 함수+전용 스타일 완전 제거 후 대체), `TherapistHomeScreen`/`SupporterHomeScreen`엔 `BacklogTaskCard` 배치 | `apps/mobile/src/lib/{weekly-tasks,transition,isp}.ts`, `apps/mobile/src/components/records/{WeeklyTaskCard,BacklogTaskCard}.tsx`(신규), `apps/mobile/src/screens/{Teacher,SocialWorker,Therapist,Supporter}HomeScreen.tsx`, `docs/14-weekly-tasks-workshop.md`(§6 신설, W-4 완료 표시) | `pnpm --filter @ongil/mobile typecheck` 통과. 에뮬레이터 부재로 렌더링 미검증. docs/14 워크숍 전체(W-1~W-4) 종결 |
| 2026-07-20 | 사용자 요청 — "기존 샘플데이터 삭제 후 종류별 15건 이상 재등록". `supabase/seed.sql`을 record_type 11종×10건 → **16종(전체 record_type)×15건**으로 전면 확장, 누락돼 있던 EDU-003(BIP)·EDU-005(ITP)·LEG-001(후견감독보고서)·LEG-002(권익옹호상담)·WEL-006(사례회의록) 5종을 신설했다. EDU-005는 청소년 전환기(personB)만, LEG-001/002는 성인기 이상(personC)만 대상으로 앱 레이어 생애주기 가드와 동일 규칙을 시드에도 반영 — social1에게 없던 LEG 도메인 권한을 personC에 신규 부여. 각 record_type 내용도 인덱스만 바뀌는 단조 반복 대신 문구·값 뱅크에서 순환 선택해 다양성을 높였고, 일부 재검토일 필드는(BIP review_date·ISP reassessment_date 등) 의도적으로 오늘 기준 D-7 이내로 배치해 방금 만든 "이번 주 처리할 일" 카드(docs/14)를 실제로 시연 가능하게 했다. **버그 수정**: 기존 스크립트는 삭제 없이 계속 append만 해서 `supabase db reset`을 반복할 때마다 레코드가 누적되는 결함이 있었음(사용자 지적과 일치) — DO 블록 안에 `DELETE FROM records/handover_notes WHERE person_id IN (...)`를 INSERT 전에 추가해 완전 멱등화. 로컬 DB에 2회 연속 실행해 240건(16×15) 그대로 유지됨(누적 안 됨)을 확인, teacher1/social1 JWT로 신규 5종 전부가 RLS 통과해 올바른 역할에게 보임을 실측 검증 | `supabase/seed.sql`(전면 재작성) | **버그 발견·수정**: WEL-006 participants 필드에 길이가 다른(3,3,2) 중첩 `ARRAY[...]` 리터럴을 써서 "multidimensional arrays must have array expressions with matching dimensions" 에러로 전체 DO 블록이 롤백되는 문제를 실행 중 발견, 길이를 맞춰 수정(PL/pgSQL의 `ARRAY[ARRAY[...],...]`는 직사각형 배열만 허용). pgTAP 스위트 재실행 결과 4개 파일(04·07·10·12)에서 실패가 있었으나 전부 내 seed 변경과 무관한 자체 픽스처(disjoint UUID, 별도 트랜잭션)의 GRANT/로직 레벨 실패라 손대지 않음(기존에도 있었을 가능성이 높은 하네스 취약점, 이번 세션 범위 밖) |
| 2026-07-20 | 사용자가 앞서 만든 "온길 기록 입력폼" 아티팩트(우측 도킹 패널 → 실제 와이드 2컬럼 레이아웃으로 수정한 버전)를 실제 플랫폼에 반영 요청. 아티팩트의 핵심 원칙 두 가지를 실제 코드에 적용: ① 콤보박스(`<select>`) 대신 라디오 타일로 — 신규 공용 컴포넌트 `components/form/ChoiceGroup.tsx`(`ChoiceGroup`=단일선택, `ChoiceCheckGroup`=복수선택) 신설, `role="radiogroup"`/`role="radio"` 접근성 패턴은 기존 `RecordForm.tsx`의 도메인 선택 라디오그룹과 동형. 전체 웹 컴포넌트의 `<select>` 19개 파일을 전수 조사해 고정 소수 enum(2~6개) 필드만 골라 8개 파일 9곳을 교체: BipForm(행동 기능+기능평가 근거 체크), IepWizard(전환 목표 영역), TherapyPlanWizard(치료 유형), TransitionPlanWizard(훈련 진행 상태), IspWizard(사정 도구), GuardianshipReportWizard(보고 구분+후견 유형), AdvocacyConsultationForm(상담 유형). 당사자 선택처럼 목록 길이가 가변적인 필드(대상 학생·연결 IEP 목표 등)는 원래 설계 의도대로 `<select>`를 그대로 유지 — 라디오 타일은 옵션이 적고 고정된 enum에만 적용하는 원칙을 코드에도 일관되게 반영. ② 당사자(SELF-001) AAC 이모티콘 방식은 조사 결과 실제 `ExpressWizard.tsx`가 이미 `IconOption` 컴포넌트로 구현돼 있어(72px 아이콘·56px+ 터치타겟, 기분/식사/활동/건강 전부 이모지 타일) 추가 작업이 필요 없었음 — 아티팩트가 실제 구현을 독립적으로 재발견한 셈 | `apps/web/src/components/form/ChoiceGroup.tsx`(신규), `apps/web/src/components/{teacher/{BipForm,IepWizard},therapist/TherapyPlanWizard,social-worker/{TransitionPlanWizard,IspWizard,GuardianshipReportWizard,AdvocacyConsultationForm}}.tsx` | `pnpm typecheck`·`build` 통과. `EvalReportForm`의 평가 시점 선택은 이미 자체 버튼 스타일(제출된 평가 유형 비활성화 로직 포함)이라 손대지 않음 — `ChoiceGroup`은 옵션별 개별 disable을 지원하지 않아 그 커스텀 로직을 대체하면 오히려 퇴보였을 것 |
| 2026-07-20 | "기록 작성화면의 와이드 레이아웃 기준을 IEP 기록의 폭에 맞춰서" 요청 — IEP(`max-w-6xl` + `lg:grid-cols-[1fr_340px]`)를 기준으로 전 기록 작성화면 폭을 통일. 2026-07-20 이른 시각의 와이드 레이아웃 라운드에서 "짧은 일상 기록엔 사이드바를 억지로 붙이지 않는다"는 판단으로 `max-w-3xl`(사이드바 없음)에 남겨뒀던 3개 화면 — `ObservationForm`(EDU-002)·`CaseConferenceForm`(WEL-006)·`AdvocacyConsultationForm`(LEG-002) — 을 이번 요청으로 그 판단을 뒤집고 IEP와 동일한 `max-w-6xl`+2컬럼 구조로 재구성했다. 왼쪽엔 필드를 fieldset 카드로 묶고, 오른쪽 사이드바엔 "온길 기록 입력폼" 아티팩트를 참고해 도메인 톤 안내 배너(확인절차 불필요 안내, LEG-002는 생애주기 가드 배너)·`TargetPersonBanner`·저장/취소 버튼을 배치했다. `RecordForm.tsx`(GEN-001)도 `max-w-5xl`→`max-w-6xl`로 맞춰 완전히 통일 — 내부 그리드는 이미 `1fr_340px`로 IEP와 동일해 폭만 조정하면 됐다. `SessionNoteForm`·`EvalReportForm`(이미 `max-w-6xl`, 각자 고유 사이드바/비교표 보유)은 변경 없음 | `apps/web/src/components/{teacher/ObservationForm,social-worker/CaseConferenceForm,social-worker/AdvocacyConsultationForm,guardian/RecordForm}.tsx` | `pnpm typecheck`·`build` 통과. 이로써 기록 작성화면 16개 중 사이드바 없이 좁게 남은 화면은 0개 — `RecordTypePicker`(입력폼이 아니라 유형 선택 메뉴)만 의도적으로 `max-w-4xl` 유지 |
| 2026-07-20 | 사용자가 "행동중재계획(BIP) 입력폼이 저장/취소 사이드바가 분리되면 오히려 좁아 보인다, 다른 기록들도 같은 문제일 듯하니 확인해달라"고 보고 — Claude-in-Chrome이 이 세션에서도 localhost 스크린샷 캡처에 3회 연속 실패해(새 탭 재시도 포함) 실제 렌더링 확인이 불가능한 상태로, frontend-dev 에이전트에게 IEP·ISP를 기준으로 기록 작성화면 16개 전체를 코드 레벨(그리드/flex 상호작용 포함)로 정밀 대조하도록 위임했다. **결론**: BipForm 자체는 그리드 클래스가 IEP와 문자 그대로 일치해 CSS 박스 모델상 폭이 좁아질 구조적 원인을 찾지 못함(추정이 아니라 grid 트랙 산정 원리로 검증) — 다만 진짜 구조적 결함 2건을 별도로 발견해 수정: ① `SessionNoteForm.tsx`가 `gap-4 lg:grid-cols-[1fr_300px]`(sidebar 300px, `lg:items-start` 없음)로 다른 15개 화면의 `gap-6 lg:grid-cols-[1fr_340px] lg:items-start` 표준에서 벗어나 있었음 — 표준에 맞춰 수정하고 `<aside>`에 `lg:sticky lg:top-6` 추가 ② `EvalReportForm.tsx`는 지난 라운드 changelog가 "이미 max-w-6xl에 고유 사이드바 보유"라고 잘못 기록했던 것과 달리 실제로는 그리드/사이드바 구조 자체가 전혀 없는 단일 세로 컬럼이었음 — IEP 패턴(왼쪽 fieldset 카드들 + 오른쪽 sticky 액션 카드)으로 완전히 재구성(상태·핸들러·로직은 손대지 않고 JSX 구조만 이동). `RecordForm.tsx`에는 다른 화면엔 있던 `min-h-full`이 빠져 있던 것도 함께 보완(세로 채움만 영향, 폭과 무관) | `apps/web/src/components/therapist/{SessionNoteForm,EvalReportForm}.tsx`, `apps/web/src/components/guardian/RecordForm.tsx` | `pnpm typecheck`·`build` 통과(직접 재확인 완료). **미해결**: BipForm이 실제 브라우저에서 왜 좁아 보이는지는 이번에도 라이브 렌더링 확인을 못 해 완전히 규명하지 못함 — 코드가 IEP와 동일하므로 순수 CSS 버그일 가능성은 낮고, 사용자가 본 것이 (a) 개선 전 캐시된 화면이었거나 (b) 단일 밀집 카드(BipForm) vs 여러 개로 시각적으로 분리된 카드(IEP)의 지각적 차이일 가능성이 있음 — 브라우저 자동화가 복구되면 최우선으로 실제 스크린샷 대조 필요 |
| 2026-07-20 | 사용자가 "PM이 UIUX·디자이너·퍼블리셔·프론트엔드개발자로 CTO팀을 구성해 ISP 작성화면을 기준으로 모든 기록 작성화면 레이아웃을 세밀하게 검증·재검증까지 마무리해달라"고 요청 — `IspWizard.tsx` 원문을 직접 읽어 6가지 규칙(최외곽 max-w-6xl/grid gap-6·1fr_340px/좌측 gap-6+fieldset 카드 분리/사이드바 gap-4 sticky/도메인 안내배너/액션카드 순서)으로 정식화한 뒤 frontend-dev 에이전트에게 16개 화면 전체를 문자 단위로 1차 대조→수정→2차 재검증까지 위임했다. 이번에도 4건의 실제 불일치를 발견·수정: ① `SessionNoteForm`이 유일하게 `<section>+<h2>` 패턴을 쓰고 있었고 저장/취소 버튼도 그리드 바깥 별도 영역에 있어 사이드바 sticky 원칙이 전혀 적용 안 되고 있었음 — `<fieldset>+<legend>`로 교체, 버튼을 사이드바 액션카드 안으로 이동하고 `TargetPersonBanner` 신규 추가 ② `JournalWizard`가 좌측/사이드바 모두 `gap-5`(표준은 좌측 gap-6·사이드바 gap-4)를 쓰고 있었음 ③ `RecordForm`(GEN-001)이 좌측 컬럼에 `flex flex-col gap-6` 래퍼와 `fieldset`/`legend`가 아예 없었음(카드 하나가 그리드 자식으로 직결) ④ `PermissionGrantWizard`가 3곳에서 `<section>+<h2>`를 쓰고 있었음. 에이전트 완료 후 메인 세션이 4개 파일 diff를 직접 재검토하고 `pnpm typecheck`·`build`를 독립적으로 재실행해 통과를 재확인(에이전트 보고를 그대로 신뢰하지 않고 이중 검증) | `apps/web/src/components/{therapist/SessionNoteForm,supporter/JournalWizard,guardian/RecordForm,guardian/PermissionGrantWizard}.tsx` | `pnpm typecheck`·`build` 통과(메인 세션 직접 재확인 완료). 이로써 16개 기록 작성화면 전부가 ISP 기준 6가지 규칙에 문자 단위로 일치 — 브라우저 렌더링 확인은 이번에도 Claude-in-Chrome이 localhost 캡처에 실패해 미수행, 코드 대조·빌드 통과로만 뒷받침됨 |
| 2026-07-20 | **BIP가 계속 좁게 보이는 문제의 진짜 근본 원인 규명·수정** — 여러 라운드의 코드 대조(className 문자 단위 일치 확인)로도 원인을 못 찾자 사용자가 직접 스크린샷 4장 + Chrome DevTools Computed 탭 값(`max-width: 1152px`인데 실제 `width: 746.9px`, 취소선 없음)을 제공. 이 단서로 CSS Flexbox의 알려진 함정을 역추적: 모든 기록 작성화면의 최외곽 `<div className="mx-auto flex min-h-full max-w-6xl flex-1 flex-col">`가 `<main className="flex flex-1 flex-col ...">`(세로 방향 flex)의 자식인데, **가로 마진이 둘 다 `auto`(mx-auto)인 flex item은 flexbox 스펙상 부모 폭에 stretch되지 않고 자기 콘텐츠 기준 shrink-to-fit으로 계산된다** — ISP·IEP는 고정 px 그리드 컬럼(`sm:grid-cols-[1fr_1fr_110px_150px_auto]` 등) 덕에 우연히 콘텐츠 자체가 넓어 거의 `max-w-6xl`까지 채워졌을 뿐이고, BIP처럼 상대적으로 "얇은" 콘텐츠 구조인 화면은 콘텐츠 크기만큼만 좁게 줄어들었던 것 — **소스 코드가 16개 전부 byte-identical하다는 이전 검증 결과가 오히려 정확했고(코드 차이가 아니었음), 이 공통 패턴 자체에 내재된 CSS 버그**였다. 최외곽 div에 `w-full` 추가(`mx-auto flex w-full min-h-full max-w-6xl flex-1 flex-col`)로 명시적 100% 폭을 강제해 shrink-to-fit 대신 항상 꽉 채운 뒤 max-width로 캡되도록 16개 파일 전부 일괄 수정 | `apps/web/src/components/{guardian/{PermissionGrantWizard,PersonRegisterWizard,RecordForm},social-worker/{AdvocacyConsultationForm,CaseConferenceForm,GuardianshipReportWizard,IspWizard,TransitionPlanWizard},supporter/JournalWizard,teacher/{BipForm,IepWizard,ItpWizard,ObservationForm},therapist/{EvalReportForm,SessionNoteForm,TherapyPlanWizard}}.tsx` | `pnpm typecheck`·`build` 통과(직접 재확인). **교훈**: 이전 라운드들의 "className 문자 단위 대조"는 진단 방법 자체가 틀렸음 — 소스가 동일해도 렌더링이 다를 수 있는 CSS 상호작용(flexbox auto-margin이 stretch를 막는 경우)이 있다는 걸 놓쳤다. 결국 사용자가 제공한 실제 브라우저 스크린샷+DevTools Computed 값이 결정적 증거였고, 이 세션 내내 반복된 "Claude-in-Chrome이 localhost를 못 띄운다"는 환경 한계 때문에 조기에 이 방식으로 전환하지 못한 점이 진단 지연의 핵심 원인이었다. 브라우저 자동화가 복구되기 전까지, 유사한 "코드는 같은데 렌더링이 다르다" 류 버그는 CSS 스펙 상호작용(flexbox stretch, percentage-of-indeterminate-ancestor 등)을 먼저 의심할 것 |
| 2026-07-20 | G-30 권한 관리 화면(`/persons/[id]/permissions`)에 이해관계자별 우측 관리 메뉴 신설 — 사용자에게 "수정"의 의미(개별 셀 클릭 유지 + 별도 일괄편집 다이얼로그 vs 클릭 방식 자체 교체)와 "회수" 범위(도메인별 vs 전체 일괄)를 AskUserQuestion으로 확인해 "드롭다운(⋮ 메뉴)으로 도메인별 일괄 수정" + "전체 도메인 한번에 회수"로 확정. 매트릭스 표 마지막 열에 "관리" 컬럼 추가, 행마다 `DropdownMenu`(⋮)로 "✏️ 수정"(6개 도메인 접근수준을 새 `Dialog`에서 한꺼번에 골라 "저장" 한 번에 반영)·"🗑️ 전체 회수"(confirm 후 즉시 전체 도메인 회수) 제공. 기존 셀 직접 클릭(즉시 순환·저장) 방식은 그대로 유지 — 두 경로가 공존. 신규 서버 액션 `updateGranteePermissions`(레벨별 회수/UPSERT 일괄 처리, edit 진입 시 valid_until 가드레일은 `cyclePermissionCell`과 동일 로직을 `resolveEditValidUntil`로 추출해 공유)·`revokeAllPermissions`(그 이해관계자의 활성 권한 전부 `is_active=false`) 신설. `components/ui/dialog.tsx` 신규(다른 UI 프리미티브와 동형으로 `@base-ui/react/dialog` 기반, 프로젝트 최초의 Dialog 컴포넌트) | `apps/web/src/app/(app)/persons/[id]/permissions/actions.ts`, `apps/web/src/components/guardian/PermissionMatrix.tsx`, `apps/web/src/components/ui/dialog.tsx`(신규) | `pnpm typecheck`·`build` 통과. 실제 렌더링 확인은 이번에도 Claude-in-Chrome이 localhost 캡처에 실패해 미수행 — 다음 세션에서 브라우저 확인 필요 |
| 2026-07-20 | 위 권한 관리 라운드 즉시 후속 피드백 3건 반영. ① "수정 선택 안 해도 셀 클릭하면 바로 자동저장된다" — 매트릭스 셀을 클릭 가능한 버튼(즉시 `cyclePermissionCell` 순환·저장)에서 읽기 전용 배지로 전환, 변경은 "수정"(다이얼로그+저장)·"전체 회수"(확인+즉시) 두 명시적 경로로만 가능하게 통일 — 이제 웹에서 아무도 안 쓰는 `cyclePermissionCell`과 전용 헬퍼(`CYCLE_ORDER`/`nextLevel`/`CycleResult`)는 삭제(모바일은 `lib/permissions.ts`에 독립 구현이 있어 무관). ② "관리의 수정/전체회수 바로 노출해줘" — ⋮ 드롭다운 한 겹을 없애고 행마다 "✏️ 수정"·"🗑️ 전체 회수" 버튼을 바로 노출. ③ "매트릭스에 권한기한도 표시, 수정 시 도메인별 또는 일괄 기한 설정" — 매트릭스 셀 배지 안에 `~YYYY-MM-DD` 기한을 작은 글씨로 함께 표시(기존엔 `title` 툴팁에만 있어 안 보였음), "수정" 다이얼로그에 도메인별 `DateField` + 상단 "기한 일괄 적용"(날짜 선택 후 버튼 클릭 시 레벨이 none이 아닌 도메인 전체에 그 날짜를 복사) 신설. `updateGranteePermissions`가 `validUntil?: string\|null`을 받도록 확장 — 명시적으로 넘어오면 그 값(빈 문자열은 무기한), 안 넘어오면 기존 값 유지, edit인데 결과가 비면 기존 가드레일(`resolveEditValidUntil`)로 자동 부여해 "edit 무기한 금지" 원칙은 그대로 지킨다 | `apps/web/src/components/guardian/PermissionMatrix.tsx`, `apps/web/src/app/(app)/persons/[id]/permissions/{actions.ts,page.tsx}` | `pnpm typecheck`·`build` 통과. 이번에도 브라우저 렌더링 실확인은 못함 |
| 2026-07-20 | 사용자 요청 — "기록관리(G-20)에서 기록 클릭 시 우측 상세도 입력폼과 같은 형식으로, 먼저 치료계획서(MED-005)부터". 지금까지 `RecordManager.tsx`(G-20 split-pane)의 우측 상세는 모든 record_type을 `RecordContentView`(범용 key-value 나열)로 렌더했는데, 이걸 `TherapyPlanWizard`(작성 폼)와 동일한 fieldset 카드 구성(대상·기본정보 / 초기 평가 / 치료 목표 / 회기 계획, 라벨·순서 동일)의 읽기 전용 뷰로 교체 — 신규 컴포넌트 `TherapyPlanReadView.tsx`. `StructuredBody`에 `recordType` prop을 추가해 `MED-005`일 때만 이 뷰를 쓰고 나머지는 기존 `RecordContentView`로 폴백(다른 record_type도 같은 패턴으로 점진 확장 예정) | `apps/web/src/components/records/TherapyPlanReadView.tsx`(신규), `apps/web/src/components/guardian/RecordManager.tsx` | `pnpm typecheck`·`build` 통과, 브라우저 실확인은 미수행. 다음 확장 후보는 사용자가 지목할 다른 record_type(IEP·ISP·BIP 등) |
| 2026-07-20 | 위 치료계획서 상세보기를 "세로로만 배열하지 말고 표 형식도 섞어서 세련되게" 피드백으로 재설계(상단 요약 바 + 기본정보 가로 정의목록 표 + 목표 목록은 실제 `<table>`) 후, "모든 기록 상세보기도 치료계획서처럼" 요청으로 나머지 13개 record_type 전체에 동일 패턴을 frontend-dev 에이전트로 확대 적용. 신규 ReadView 13개(IEP·관찰기록·BIP·ITP·회기일지·평가보고서·ISP·사례회의록·전환계획·후견감독보고서·권익옹호상담·활동지원일지·자기표현), 각 record_type의 실제 위저드·Zod 스키마를 에이전트가 직접 읽어 필드명을 맞춤. `RecordManager.tsx`의 분기를 단일 MED-005 체크에서 14개 record_type 조회 맵으로 확장(WEL-005·GEN-001은 기존 폴백 유지) | `apps/web/src/components/records/{IepReadView,ObservationReadView,BipReadView,ItpReadView,SessionNoteReadView,EvalReportReadView,IspReadView,CaseConferenceReadView,TransitionPlanReadView,GuardianshipReportReadView,AdvocacyConsultationReadView,JournalReadView,SelfExpressionReadView}.tsx`(전부 신규), `apps/web/src/components/guardian/RecordManager.tsx` | `pnpm typecheck`·`build` 메인 세션이 독립 재실행해 재확인(에이전트 보고만 신뢰하지 않음). SELF-001은 6도메인 밖이라 도메인 색상 대신 중립 primary 톤 사용, TRA-001은 기존 `RoadmapProgress`를 읽기 전용으로 재사용, Journal의 서버 계산 `service_hours`는 스키마 외 필드로 별도 타입 확장 — 전부 코드 검토로 확인. 브라우저 실렌더링 확인은 이번에도 미수행 |
| 2026-07-20 | 사용자 요청 — "타임라인 스트림 뷰를 생애주기별로 분류하고 목록 선택 시 우측에 상세보기, 레인 뷰 카드에 도메인·작성자 표시". 이어진 3건의 즉시 후속 피드백(응급대응정보를 목록과 분리해 상단 고정, 스트림 카드도 기록관리처럼 2줄로) 전부 같은 라운드에서 반영. ① `getTimeline()`(records/iep/actions.ts)에 `author:users!records_author_id_fkey(full_name)` 조인 추가, `TimelineItem.authorName` 신설 ② `RecordTimelineCard`·`TimelineLane`에 작성자·도메인 표시 추가 ③ 기록관리(RecordManager)의 우측 상세 로직(조회·확인·ReadView 분기)을 `RecordDetailPane.tsx`(신규, `components/records/`)로 분리해 기록관리·타임라인 공용화 — `personId`를 주면 수정 링크, 안 주면 생략(전문직군 공용 `/timeline`은 그 기록 수정 권한 체계가 보호자 전용 edit 라우트와 달라 `personId`를 넘기지 않음, 보호자 전용 `/persons/[id]/timeline`만 넘김) ④ `TimelineStream`이 `birthDate` 있으면 생애주기 단계별로 섹션 분류(각 단계 첫 등장 순서 = 최신 단계부터), `onSelect`로 좌측 목록 클릭 시 우측 `RecordDetailPane` 연동 ⑤ `TimelineView`가 좌(목록)·우(상세) 분할 그리드로 재구성(스트림 뷰만 — 레인 뷰는 가로로 넓어 분할과 안 맞아 제외) ⑥ 응급대응정보(PinnedCard)를 스트림 목록에서 완전히 분리해 필터·뷰 전환과 무관하게 항상 최상단에 고정 노출 ⑦ `RecordTimelineCard`를 기록관리 좌측 목록과 동일한 2줄 행(제목·도메인·임시저장배지 / 작성자·날짜·태그)으로 재설계, `MilestoneCard`(이정표)는 시각적 강조를 위해 카드 형태 유지 | `apps/web/src/app/(app)/records/iep/actions.ts`, `apps/web/src/components/timeline/{RecordTimelineCard,TimelineLane,TimelineStream,TimelineView}.tsx`, `apps/web/src/components/records/RecordDetailPane.tsx`(신규), `apps/web/src/components/guardian/RecordManager.tsx`(RecordDetailPane 사용으로 축소), `apps/web/src/app/(app)/persons/[id]/timeline/page.tsx`(personId 전달) | `pnpm typecheck`·`build` 통과. 브라우저 실렌더링 확인은 이번에도 미수행 |
| 2026-07-21 | 타임라인 스트림 뷰가 PinnedCard(응급정보) 높이가 들쭉날쭉해 화면 아래로 넘치는 문제 신고 → "접근 로그도 화면 전체가 스크롤된다, 모든 메뉴가 기록관리처럼 헤더·사이드바는 고정하고 내용만 스크롤되게 해달라"로 요청이 확대돼 **루트 레이아웃 구조 자체를 수정**했다. 근본 원인: `app/layout.tsx`의 `<body>`가 `min-h-full`(콘텐츠가 넘치면 그만큼 늘어남)이라 화면 전체가 스크롤됐고, `(app)/layout.tsx`의 `<main>`도 높이가 진짜로 정해져 있지 않아 RecordManager 등 개별 화면이 `calc(100vh-매직넘버)`로 억지로 자기 높이를 추측해야 했다(위쪽 요소 높이가 바뀌면 깨짐 — 이번 PinnedCard 사례가 그 증거). `<body>`를 `h-full`로, `(app)/layout.tsx`의 세 분기(person/guardian/기타) 전부 `h-full min-h-0` 체인 + `main`을 `min-h-0 flex-1 overflow-y-auto`(실제 스크롤 컨테이너)로 통일 — 이 한 번의 수정으로 접근 로그처럼 손대지 않은 화면도 자동으로 "헤더 고정·목록만 스크롤"이 됐고, `RecordManager`·`TimelineView`는 이제 `calc(100vh-N)` 매직넘버 대신 `h-full`/`flex-1 min-h-0`만 쓰도록 단순화했다 | `apps/web/src/app/layout.tsx`, `apps/web/src/app/(app)/layout.tsx`, `apps/web/src/components/guardian/RecordManager.tsx`, `apps/web/src/components/timeline/TimelineView.tsx` | `pnpm typecheck`·`build` 통과. 브라우저 실렌더링 확인은 여전히 미수행 — 특히 이번 변경은 전 화면에 영향을 주는 구조적 수정이라 다음 브라우저 접근 가능 시 최우선 스팟체크 대상(당사자 모드·보호자·전문직군 3개 레이아웃 분기 전부, 응급정보 카드가 긴 경우, 좁은 화면 반응형 스택 포함) |
| 2026-07-21 | 활동지원사 일지 작성 중 임시저장 시 "Invalid enum value. Expected 'good'\|'sick'\|'tired', received 'normal'" 오류 신고 — `supportJournalSchema.health_status`는 `good/sick/tired` 3종인데 `supabase/seed.sql`이 `ARRAY['good','normal','tired']`로 잘못된 값을 심어놨던 게 원인. JournalWizard의 "이전 일지 자동 불러오기"가 그 잘못된 값을 그대로 상태에 채운 뒤 임시저장(`submitSupportJournal`이 draft 여부와 무관하게 항상 `supportJournalSchema.safeParse`로 전체 검증)에서 막힌 것 — seed.sql을 `'sick'`으로 수정하고, 로컬 DB에 이미 들어간 기존 5건도 직접 UPDATE로 정정. 근본 원인과 별개로 `JournalWizard.applyPrevious()`가 이전 기록의 `health_status`/`meal_status`를 유효성 검사 없이 그대로 신뢰하고 있던 방어 공백도 함께 메움(HEALTHS/MEALS 목록에 있는 값일 때만 반영) — DB에 남아있을 수 있는 다른 레거시 데이터에도 안전하도록. 모바일(`JournalComposeScreen.tsx`)의 "이전 일지 불러오기"는 활동 카테고리만 복사하고 health/meal은 건드리지 않아 애초에 영향 없음을 확인 | `supabase/seed.sql`, `apps/web/src/components/supporter/JournalWizard.tsx` | `pnpm typecheck`·`build` 통과. 로컬 DB 직접 조회로 잘못된 값 5건 확인 후 UPDATE로 정정, 재조회로 0건 확인 |
